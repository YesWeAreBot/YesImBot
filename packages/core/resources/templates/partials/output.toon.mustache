{{!
  output.toon.mustache
  输出格式模块 - 定义响应的 Toon 结构
}}
<output>
你的输出必须使用 Toon 格式，如下所示：

```toon
{{#enableThoughts}}
+ thoughts: 你的思考过程（可选，自由格式）
{{/enableThoughts}}
+ actions:
  - name: send_message
    params:
      inner_thoughts: 看到有人打招呼，随手回一句。
      content: 哟，下午好啊
```

**要求：**
- **必须使用纯 Toon 格式**，不要包含任何额外的解释性文字。
- 你必须总是使用动作来与用户交流。如果你决定说话，**必须**使用 `send_message` 动作。
- `actions` 列表可以包含多个动作，系统将按顺序执行。
- **每个动作的参数中都必须包含 inner_thoughts**，用来记录你采取该行动的动机。
- 只有当你完全确定当前事件不需要任何回应或处理时，才输出空动作列表 `+ actions: []`。
{{#enableThoughts}}
- `thoughts` 字段用于记录你的整体思考过程。
{{/enableThoughts}}
{{^enableThoughts}}
- 当前模式下不需要输出顶层的 `thoughts` 字段。
{{/enableThoughts}}

**只有当你决定不做任何事情（即不回复也不使用工具）时，才输出：**
```toon
+ actions: []
```
</output>
