# Domain Docs

Single-context layout — one `CONTEXT.md` at the repo root for domain language, and `docs/adr/` for architectural decision records.

## Consumer rules

- Read `CONTEXT.md` before any task that touches domain concepts or business logic.
- Read `docs/adr/` before making architectural changes or when confused about past decisions.
- If `CONTEXT.md` does not exist yet, skip gracefully — it will be created when the project's domain language stabilizes.

## Current state

- `CONTEXT.md` — not yet created
- `docs/adr/` — not yet created
