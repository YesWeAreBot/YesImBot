---
phase: 27-scope-deletion-module-migration
plan: 03
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - core/src/services/plugin/types.ts
  - core/src/services/plugin/service.ts
  - core/src/services/agent/service.ts
  - core/src/services/agent/loop.ts
autonomous: true
requirements: [CTX-05]

must_haves:
  truths:
    - "ToolExecutionContext has bare platform/channelId fields instead of scope: Scope"
    - "agent/service.ts buildPercept constructs percept with bare fields from event"
    - "agent/service.ts handleEvent reads isDirect from event.runtime.session"
    - "agent/loop.ts passes ChannelKey objects to horizon.buildView, trait.analyze, skill.resolve"
    - "agent/loop.ts recordAgentResponse uses bare platform/channelId fields"
    - "plugin/service.ts fallback context uses bare platform/channelId fields"
  artifacts:
    - path: "core/src/services/plugin/types.ts"
      provides: "Updated ToolExecutionContext with bare fields"
    - path: "core/src/services/plugin/service.ts"
      provides: "Updated fallback context"
    - path: "core/src/services/agent/service.ts"
      provides: "Updated buildPercept, handleEvent, reportError"
    - path: "core/src/services/agent/loop.ts"
      provides: "Updated all percept.scope.* accesses"
  key_links:
    - from: "core/src/services/agent/loop.ts"
      to: "core/src/services/horizon/service.ts"
      via: "horizon.buildView({ platform: percept.platform, channelId: percept.channelId }, ...)"
      pattern: "buildView.*platform.*percept"
    - from: "core/src/services/agent/service.ts"
      to: "core/src/services/agent/loop.ts"
      via: "buildPercept creates Percept consumed by loop.run"
      pattern: "buildPercept"
---

<objective>
Migrate Agent module (service, loop) and Plugin module (types, service) from Scope to bare platform/channelId fields.

Purpose: Complete the top layer of the bottom-up migration. After this plan, zero Scope references remain in the codebase and `yarn build` passes.
Output: Fully migrated agent and plugin modules, clean build.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-scope-deletion-module-migration/27-RESEARCH.md
@.planning/phases/27-scope-deletion-module-migration/27-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan depends on -->

From core/src/services/shared/types.ts (after Plan 01):
```typescript
export type ChannelKey = { platform: string; channelId: string };

export interface Percept {
  id: string;
  traceId: string;
  type: TriggerType;
  platform: string;      // was: scope.platform
  channelId: string;     // was: scope.channelId
  timestamp: Date;
  metadata?: Record<string, unknown>;
}
```

From core/src/services/horizon/types.ts (after Plan 01):
```typescript
export interface HorizonMessageEvent {
  platform: string;      // was: scope.platform
  channelId: string;     // was: scope.channelId
  timestamp: Date;
  payload: { ... };
  triggerType: TriggerType;
  runtime?: { session: Session };
}
```

From core/src/services/horizon/manager.ts (after Plan 01):
```typescript
// recordAgentResponse now expects: { platform: string; channelId: string; timestamp; data }
// markAsActive now expects: (key: ChannelKey, before?: Date)
// archiveStale now expects: (key: ChannelKey, olderThanMs: number)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Plugin types and Agent service</name>
  <files>
    core/src/services/plugin/types.ts
    core/src/services/plugin/service.ts
    core/src/services/agent/service.ts
  </files>
  <action>
**plugin/types.ts:**
1. Change import: `import type { Percept, Scope } from "../shared/types"` → `import type { Percept } from "../shared/types"`
2. Update `ToolExecutionContext`: remove `scope: Scope`, add `platform: string` and `channelId: string` as first two fields. Keep `session`, `bot`, `percept`, and index signature.

**plugin/service.ts:**
1. Find the fallback context line: `context ?? { scope: {} }` → `context ?? { platform: "", channelId: "" }`

**agent/service.ts:**
1. Remove `Scope` from any imports (it imports `Percept` from shared/types — keep that, just ensure no `Scope` reference)
2. `handleEvent()` method:
   - `event.scope.platform` → `event.platform`, `event.scope.channelId` → `event.channelId` for channelKey construction
   - `event.scope.isDirect` → `event.runtime?.session?.isDirect ?? false` for rate limiter bucket selection
   - `const bucketKey = \`${event.scope.platform}:${userId}\`` → `const bucketKey = \`${event.platform}:${userId}\``
3. `buildPercept()` method:
   - Percept construction: remove `scope: event.scope`, add `platform: event.platform, channelId: event.channelId`
   - toolCtx construction: remove `scope: event.scope`, add `platform: event.platform, channelId: event.channelId`
4. `executeDeferredJudgment()` method:
   - `horizon.buildView(built.percept.scope, ...)` → `horizon.buildView({ platform: built.percept.platform, channelId: built.percept.channelId }, ...)`
5. `reportError()` method:
   - `percept.scope.channelId` → `percept.channelId`
  </action>
  <verify>
    <automated>cd /home/workspace/Athena && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>plugin/types.ts has bare fields in ToolExecutionContext. agent/service.ts reads isDirect from session. buildPercept constructs flat percept. reportError uses percept.channelId.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Agent loop and final build verification</name>
  <files>
    core/src/services/agent/loop.ts
  </files>
  <action>
**agent/loop.ts** — This file has 7+ usages of `percept.scope.*` that all need updating:

1. Line ~62: `horizon.buildView(percept.scope, {...})` → `horizon.buildView({ platform: percept.platform, channelId: percept.channelId }, {...})`
2. Line ~73: `trait.analyze(percept.scope, view)` → `trait.analyze({ platform: percept.platform, channelId: percept.channelId }, view)`
3. Line ~74: `skill.resolve(signals, percept.scope)` → `skill.resolve(signals, { platform: percept.platform, channelId: percept.channelId })`
4. Line ~147: `const channelKey = \`${percept.scope.platform}:${percept.scope.channelId}\`` → `const channelKey = \`${percept.platform}:${percept.channelId}\``
5. Lines ~317, ~358: `scope: percept.scope` in `recordAgentResponse` calls → `platform: percept.platform, channelId: percept.channelId`
6. Line ~380: `horizon.events.markAsActive(percept.scope, ...)` → `horizon.events.markAsActive({ platform: percept.platform, channelId: percept.channelId }, ...)`
7. Line ~383: `horizon.events.archiveStale(percept.scope, ...)` → `horizon.events.archiveStale({ platform: percept.platform, channelId: percept.channelId }, ...)`

After all changes, run full build verification:
- `yarn typecheck` (or `npx tsc --noEmit`) must pass with zero errors
- `grep -r "Scope" core/src/services/` should return zero hits for the interface (only "ChannelKey" references and unrelated uses of the word "scope" in comments/DB column names)
  </action>
  <verify>
    <automated>cd /home/workspace/Athena && yarn build 2>&1 | tail -20</automated>
  </verify>
  <done>agent/loop.ts compiles with zero percept.scope references. `yarn build` passes. No Scope interface references remain in the codebase. Phase 27 migration complete.</done>
</task>

</tasks>

<verification>
1. `yarn build` passes with zero TypeScript errors
2. `grep -rn "interface Scope" core/src/` returns no results
3. `grep -rn "scope: Scope" core/src/` returns no results
4. `grep -rn "\.scope\." core/src/services/agent/` returns no results (no more percept.scope.* or event.scope.*)
5. `grep -rn "ChannelKey" core/src/services/shared/types.ts` confirms the type exists
6. `grep -rn "percept\.platform" core/src/services/agent/loop.ts` confirms bare field access
</verification>

<success_criteria>
- ToolExecutionContext has platform/channelId bare fields (no scope: Scope)
- plugin/service.ts fallback uses bare fields
- agent/service.ts buildPercept creates flat Percept
- agent/service.ts handleEvent reads isDirect from event.runtime.session
- agent/loop.ts has zero percept.scope references
- `yarn build` passes with zero errors
- No Scope interface definition exists anywhere in codebase
</success_criteria>

<output>
After completion, create `.planning/phases/27-scope-deletion-module-migration/27-03-SUMMARY.md`
</output>
