---
phase: 27-scope-deletion-module-migration
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - core/src/services/trait/types.ts
  - core/src/services/trait/service.ts
  - core/src/services/trait/detectors/heat.ts
  - core/src/services/trait/detectors/scene.ts
  - core/src/services/skill/service.ts
autonomous: true
requirements: [CTX-03, CTX-04]

must_haves:
  truths:
    - "TraitDetector.detect signature uses ChannelKey instead of Scope"
    - "TraitAnalyzer.analyze signature uses ChannelKey instead of Scope"
    - "HeatTrait and SceneTrait channelKey helpers accept ChannelKey"
    - "SceneTrait.detect derives isDirect from view.environment.type instead of scope.isDirect"
    - "SkillRegistry.resolve signature uses ChannelKey instead of Scope"
  artifacts:
    - path: "core/src/services/trait/types.ts"
      provides: "Updated TraitDetector interface"
      contains: "ChannelKey"
    - path: "core/src/services/trait/service.ts"
      provides: "Updated analyze method"
    - path: "core/src/services/trait/detectors/heat.ts"
      provides: "Updated channelKey helper and detect method"
    - path: "core/src/services/trait/detectors/scene.ts"
      provides: "Updated channelKey helper, detect method, isDirect derivation"
    - path: "core/src/services/skill/service.ts"
      provides: "Updated resolve method"
  key_links:
    - from: "core/src/services/trait/types.ts"
      to: "core/src/services/shared/types.ts"
      via: "ChannelKey import"
      pattern: "import.*ChannelKey.*shared/types"
    - from: "core/src/services/trait/detectors/scene.ts"
      to: "core/src/services/horizon/types.ts"
      via: "HorizonView.environment.type for isDirect derivation"
      pattern: "environment.*type.*private"
---

<objective>
Migrate Trait module (types, service, heat detector, scene detector) and Skill module (service) from Scope to ChannelKey.

Purpose: Complete the middle layer of the bottom-up migration. These modules consume ChannelKey from shared types and are consumed by the Agent module.
Output: All trait detectors and skill resolver using ChannelKey parameters.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-scope-deletion-module-migration/27-RESEARCH.md
@.planning/phases/27-scope-deletion-module-migration/27-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that this plan depends on -->

From core/src/services/shared/types.ts (after Plan 01):
```typescript
export type ChannelKey = { platform: string; channelId: string };

export interface Percept {
  id: string;
  traceId: string;
  type: TriggerType;
  platform: string;
  channelId: string;
  timestamp: Date;
  metadata?: Record<string, unknown>;
}
```

From core/src/services/horizon/types.ts (after Plan 01):
```typescript
export interface HorizonMessageEvent {
  platform: string;
  channelId: string;
  timestamp: Date;
  payload: { messageId: string; senderId: string; senderName: string; content: string };
  triggerType: TriggerType;
  runtime?: { session: Session };
}

export interface EventQueryOptions {
  key?: ChannelKey;
  // ...
}
```

From core/src/services/horizon/types.ts (unchanged):
```typescript
export interface Environment {
  type: string;  // "private" or "group" — set by getOrCreateEnvironment
  id: string;
  name: string;
  // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Trait module (types, service, detectors) to ChannelKey</name>
  <files>
    core/src/services/trait/types.ts
    core/src/services/trait/service.ts
    core/src/services/trait/detectors/heat.ts
    core/src/services/trait/detectors/scene.ts
  </files>
  <action>
**trait/types.ts:**
1. Change import: `import type { Scope, TraitSignal } from "../shared/types"` → `import type { ChannelKey, TraitSignal } from "../shared/types"`
2. Update `TraitDetector.detect` signature: `detect(scope: Scope, view: HorizonView)` → `detect(key: ChannelKey, view: HorizonView)`

**trait/service.ts:**
1. Change import: `import type { Scope, TraitSignal } from "../shared/types"` → `import type { ChannelKey, TraitSignal } from "../shared/types"`
2. Update `analyze` method: `async analyze(scope: Scope, view: HorizonView)` → `async analyze(key: ChannelKey, view: HorizonView)`
3. Inside `analyze`: change `d.detect(scope, view)` → `d.detect(key, view)`

**trait/detectors/heat.ts:**
1. Change import: `import type { Scope, TraitSignal } from "../../shared/types"` → `import type { ChannelKey, TraitSignal } from "../../shared/types"`
2. Update `channelKey` helper: `function channelKey(scope: Scope): string` → `function channelKey(key: ChannelKey): string`. Inside: `${scope.platform}:${scope.channelId}` → `${key.platform}:${key.channelId}`
3. In `start()` event handler: `channelKey(event.scope)` → `channelKey(event)` — because `HorizonMessageEvent` now has bare `platform`/`channelId` fields, it satisfies `ChannelKey`
4. Update `detect` signature: `detect(scope: Scope, _view: HorizonView)` → `detect(key: ChannelKey, _view: HorizonView)`. Inside: `channelKey(scope)` → `channelKey(key)`

**trait/detectors/scene.ts:**
1. Change import: `import type { Scope, TraitSignal } from "../../shared/types"` → `import type { ChannelKey, TraitSignal } from "../../shared/types"`
2. Update `channelKey` helper: same as heat.ts — `function channelKey(key: ChannelKey): string`
3. In `start()` event handler: `channelKey(event.scope)` → `channelKey(event)`
4. Update `detect` signature: `detect(scope: Scope, view: HorizonView)` → `detect(key: ChannelKey, view: HorizonView)`. Inside: `channelKey(scope)` → `channelKey(key)`
5. **CRITICAL — isDirect derivation:** The line `value: scope.isDirect ? "private-chat" : "group-chat"` must change. `ChannelKey` has no `isDirect`. Derive from `view.environment?.type`: `value: view.environment?.type === "private" ? "private-chat" : "group-chat"`. The `Environment.type` is already set correctly from session data in `getOrCreateEnvironment` (Plan 01).
  </action>
  <verify>
    <automated>cd /home/workspace/Athena && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>All 4 trait files compile. TraitDetector interface uses ChannelKey. Scene detector derives isDirect from view.environment.type. Heat detector event handler uses bare event fields.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Skill module resolve method to ChannelKey</name>
  <files>
    core/src/services/skill/service.ts
  </files>
  <action>
**skill/service.ts:**
1. Change import: `import type { Scope, TraitSignal } from "../shared/types"` → `import type { ChannelKey, TraitSignal } from "../shared/types"`
2. Update `resolve` method signature: `resolve(signals: TraitSignal[], scope: Scope): SkillEffect` → `resolve(signals: TraitSignal[], key: ChannelKey): SkillEffect`
3. Inside `resolve`: change `const channelKey = \`${scope.platform}:${scope.channelId}\`` → `const channelKey = \`${key.platform}:${key.channelId}\``
4. All other references to `scope` inside `resolve` are already gone since only `platform` and `channelId` were used.
  </action>
  <verify>
    <automated>cd /home/workspace/Athena && npx tsc --noEmit --pretty 2>&1 | head -50</automated>
  </verify>
  <done>skill/service.ts compiles. resolve() accepts ChannelKey. Remaining errors only in agent/plugin files (fixed in Plan 03).</done>
</task>

</tasks>

<verification>
1. `grep -r "scope: Scope" core/src/services/trait/` returns no results
2. `grep -r "scope: Scope" core/src/services/skill/` returns no results
3. `grep -r "ChannelKey" core/src/services/trait/types.ts` shows the import and usage
4. `grep "isDirect" core/src/services/trait/detectors/scene.ts` shows derivation from view.environment.type, not scope
</verification>

<success_criteria>
- TraitDetector.detect uses ChannelKey parameter
- TraitAnalyzer.analyze uses ChannelKey parameter
- Both detectors' channelKey helpers accept ChannelKey
- SceneTrait derives isDirect from view.environment.type
- HeatTrait event handler uses bare event fields (not event.scope)
- SkillRegistry.resolve uses ChannelKey parameter
- No TypeScript errors in the 5 modified files
</success_criteria>

<output>
After completion, create `.planning/phases/27-scope-deletion-module-migration/27-02-SUMMARY.md`
</output>
