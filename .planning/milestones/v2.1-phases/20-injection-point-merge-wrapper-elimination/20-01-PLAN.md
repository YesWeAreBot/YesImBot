---
phase: 20-injection-point-merge-wrapper-elimination
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/src/services/prompt/types.ts
  - core/src/services/prompt/service.ts
  - core/src/services/agent/loop.ts
autonomous: true
requirements:
  - PROMPT-01
  - PROMPT-04

must_haves:
  truths:
    - "InjectionPoint type is 'soul | instructions | memory | extra' — old names cause compile errors"
    - "inject() throws Error on unrecognized injection point names"
    - "CACHEABLE_POINTS is derived from INJECTION_POINTS (all 4 cacheable)"
    - "loop.ts injects into 'soul' and 'instructions' instead of 'style' and 'basic_functions'"
  artifacts:
    - path: "core/src/services/prompt/types.ts"
      provides: "New InjectionPoint type and INJECTION_POINTS array"
      contains: "soul.*instructions.*memory.*extra"
    - path: "core/src/services/prompt/service.ts"
      provides: "Runtime guard in inject(), derived CACHEABLE_POINTS"
      contains: "Unrecognized injection point"
    - path: "core/src/services/agent/loop.ts"
      provides: "Migrated call sites"
      contains: '"soul"'
  key_links:
    - from: "core/src/services/prompt/types.ts"
      to: "core/src/services/prompt/service.ts"
      via: "InjectionPoint type import"
      pattern: "INJECTION_POINTS"
    - from: "core/src/services/prompt/service.ts"
      to: "core/src/services/agent/loop.ts"
      via: "inject() call with new point names"
      pattern: 'inject.*"soul"|inject.*"instructions"'
---

<objective>
Merge 6 injection points to 4 and update all call sites with type-safe guards.

Purpose: Establish the new `soul | instructions | memory | extra` type system that the compiler enforces across the entire codebase. This is the foundation for Plan 02's template elimination.
Output: Compiling codebase with new InjectionPoint type, runtime guard in inject(), and all call sites migrated.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-injection-point-merge-wrapper-elimination/20-CONTEXT.md
@.planning/phases/20-injection-point-merge-wrapper-elimination/20-RESEARCH.md
@core/src/services/prompt/types.ts
@core/src/services/prompt/service.ts
@core/src/services/agent/loop.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge InjectionPoint type and update CACHEABLE_POINTS</name>
  <files>
    core/src/services/prompt/types.ts
    core/src/services/prompt/service.ts
  </files>
  <action>
In `types.ts`:
- Change `InjectionPoint` type to `"soul" | "instructions" | "memory" | "extra"`
- Change `INJECTION_POINTS` array to `["soul", "instructions", "memory", "extra"]`

In `service.ts`:
- Replace `CACHEABLE_POINTS` hardcoded Set with: `const CACHEABLE_POINTS = new Set<InjectionPoint>(INJECTION_POINTS);` (all 4 are cacheable per user decision)
- Add runtime guard in `inject()`: check `this.injections.get(point)` — if undefined, throw `new Error(\`Unrecognized injection point: "\${point}"\`)` before the duplicate check
  </action>
  <verify>
    <automated>cd /home/workspace/Athena && npx tsc --noEmit -p core/tsconfig.json 2>&1 | head -30</automated>
    <manual>Expect compile errors ONLY in loop.ts (old point names "style", "basic_functions") — types.ts and service.ts should be clean</manual>
  </verify>
  <done>InjectionPoint is "soul | instructions | memory | extra", CACHEABLE_POINTS derived from INJECTION_POINTS, inject() throws on bad names</done>
</task>

<task type="auto">
  <name>Task 2: Migrate loop.ts call sites to new injection point names</name>
  <files>
    core/src/services/agent/loop.ts
  </files>
  <action>
In `loop.ts`:
- Change `prompt.inject(this.ctx, "style", ...)` (the styleOverride injection around line 78) to `prompt.inject(this.ctx, "soul", ...)`
- Change `prompt.inject(this.ctx, "basic_functions", ...)` (the tool schema injection around line 93) to `prompt.inject(this.ctx, "instructions", ...)`
- Update the `after: "__default_style"` ordering hint in the styleOverride injection to `after: "__default_soul"` (the default injection name will change in Plan 02, but use the new conceptual name now for consistency — if the default doesn't exist yet, the ordering hint is safely ignored by resolveOrder)

No other files need call site changes — skill/service.ts uses "extra" (unchanged) and memory/service.ts uses "memory" (unchanged).
  </action>
  <verify>
    <automated>cd /home/workspace/Athena && npx tsc --noEmit -p core/tsconfig.json 2>&1 | head -10</automated>
    <manual>Expect zero compile errors — all call sites now use valid injection point names</manual>
  </verify>
  <done>All call sites compile with new injection point names. "style" and "basic_functions" no longer appear anywhere in TypeScript source.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p core/tsconfig.json` — zero errors
2. `grep -r '"identity"\|"style"\|"control_flow"\|"basic_functions"' core/src/` — no matches (old names fully eliminated from TS source)
3. InjectionPoint type in types.ts contains exactly: soul, instructions, memory, extra
</verification>

<success_criteria>
- TypeScript compilation succeeds with zero errors
- No old injection point names remain in any .ts file under core/src/
- CACHEABLE_POINTS is derived from INJECTION_POINTS, not hardcoded
- inject() has runtime guard that throws on unrecognized point names
</success_criteria>

<output>
After completion, create `.planning/phases/20-injection-point-merge-wrapper-elimination/20-01-SUMMARY.md`
</output>
