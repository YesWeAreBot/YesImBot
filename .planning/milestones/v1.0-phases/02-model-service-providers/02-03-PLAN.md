---
phase: 02-model-service-providers
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - providers/provider-deepseek/package.json
  - providers/provider-deepseek/tsconfig.json
  - providers/provider-deepseek/src/index.ts
autonomous: true
requirements: [MODEL-03]

must_haves:
  truths:
    - "DeepSeek provider plugin registers with ModelService on load"
    - "DeepSeek provider plugin unregisters on dispose"
    - "Provider creates LanguageModel via createOpenAI with DeepSeek baseURL"
    - "User configures instanceName, apiKey, and model list in Koishi console"
  artifacts:
    - path: "providers/provider-deepseek/package.json"
      provides: "Package with @ai-sdk/openai dep (DeepSeek is OpenAI-compatible)"
      contains: "@ai-sdk/openai"
    - path: "providers/provider-deepseek/src/index.ts"
      provides: "Koishi plugin that registers DeepSeek provider with ModelService"
      exports: ["name", "inject", "Config", "apply"]
  key_links:
    - from: "providers/provider-deepseek/src/index.ts"
      to: "plugins/core/src/services/model-service.ts"
      via: "ctx['model-service'].registerProvider"
      pattern: "registerProvider"
    - from: "providers/provider-deepseek/src/index.ts"
      to: "@ai-sdk/openai"
      via: "createOpenAI with custom baseURL"
      pattern: "createOpenAI.*baseURL"
---

<objective>
Create DeepSeek provider plugin using OpenAI-compatible API pattern with custom baseURL.

Purpose: Enables calling DeepSeek models (deepseek-chat, deepseek-reasoner) through ModelService. DeepSeek API is OpenAI-compatible, so reuses createOpenAI with custom baseURL.
Output: `@yesimbot/provider-deepseek` Koishi plugin package.
</objective>

<execution_context>
@C:/Users/MiaowFISH/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MiaowFISH/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-model-service-providers/02-CONTEXT.md
@.planning/phases/02-model-service-providers/02-RESEARCH.md
@.planning/phases/02-model-service-providers/02-01-SUMMARY.md
@providers/provider-openai/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create provider-deepseek package and implement plugin</name>
  <files>
    providers/provider-deepseek/package.json
    providers/provider-deepseek/tsconfig.json
    providers/provider-deepseek/src/index.ts
  </files>
  <action>
Follow the exact same pattern as provider-openai (reference 02-02 SUMMARY or provider-openai source).

Create `providers/provider-deepseek/package.json`:
- name: `@yesimbot/provider-deepseek`
- Same structure as provider-openai (type: module, pkgroll build, same deps)
- dependencies: `@yesimbot/shared-model: "workspace:*"`, `@ai-sdk/openai: "^3.0.0"`
- peerDependencies: `koishi: "^4.0.0"`

Create `providers/provider-deepseek/tsconfig.json`:
- Same as provider-openai (extend base, reference shared-model)

Create `providers/provider-deepseek/src/index.ts`:
- `name = 'yesimbot-provider-deepseek'`
- `inject = ['model-service']`
- Config schema: same fields as OpenAI but with different defaults:
  - `baseURL` defaults to `'https://api.deepseek.com/v1'`
  - `models` defaults to `[{ id: 'deepseek-chat', capabilities: ['toolCalling', 'jsonMode', 'streaming'] }, { id: 'deepseek-reasoner', capabilities: ['streaming'] }]`
- DeepSeekProvider class implementing IModelProvider:
  - `providerType = 'deepseek'`
  - Uses `createOpenAI({ apiKey, baseURL })` — same as OpenAI since DeepSeek is OpenAI-compatible
  - `getModel(modelId)`: return `this.client(modelId)`
- apply: register provider, dispose: unregister

Run `yarn install` then `yarn turbo run build` to verify full monorepo builds.
  </action>
  <verify>
Run `cd D:/Codespace/koishi-dev/YesWeAreBot/YesImBot && yarn turbo run build` — all packages build including provider-deepseek. Verify dist/ output exists.
  </verify>
  <done>DeepSeek provider plugin builds, registers with ModelService using OpenAI-compatible createOpenAI with DeepSeek baseURL. Default models: deepseek-chat, deepseek-reasoner.</done>
</task>

</tasks>

<verification>
- `yarn turbo run build` succeeds for all packages including provider-deepseek
- provider-deepseek exports: name, inject, Config, apply
- Provider uses createOpenAI with baseURL https://api.deepseek.com/v1
- Plugin declares inject: ['model-service']
- Dispose handler unregisters provider
</verification>

<success_criteria>
1. provider-deepseek package builds successfully in monorepo
2. Plugin registers with ModelService using configured instanceName
3. Uses createOpenAI with DeepSeek baseURL (OpenAI-compatible pattern)
4. Plugin cleans up on dispose
</success_criteria>

<output>
After completion, create `.planning/phases/02-model-service-providers/02-03-SUMMARY.md`
</output>
