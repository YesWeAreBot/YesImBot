---
phase: 10-willingness-system-migration
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - plugins/core/src/services/agent/config.ts
  - plugins/core/src/services/agent/service.ts
  - plugins/core/src/services/agent/index.ts
  - plugins/core/src/index.ts
autonomous: true
requirements: [WILLING-01, WILLING-02, WILLING-03]

must_haves:
  truths:
    - "AgentCore creates WillingnessEngine on start and runs decay timer via ctx.setInterval"
    - "Every incoming percept updates willingness state (not just gated ones) for accurate heat detection"
    - "After successful reply, AgentCore calls engine.recordBotReply for fatigue tracking"
    - "Old willingness config fields removed from AgentCoreConfig and root Config"
    - "New nested willingness Schema group appears in root Config"
    - "processMessage is synchronous — no async LLM call in gating path"
  artifacts:
    - path: "plugins/core/src/services/agent/config.ts"
      provides: "AgentCoreConfig with willingness field referencing WillingnessConfig"
      contains: "WillingnessConfig"
    - path: "plugins/core/src/services/agent/service.ts"
      provides: "AgentCore using WillingnessEngine"
      contains: "WillingnessEngine"
    - path: "plugins/core/src/services/agent/index.ts"
      provides: "Updated exports"
      contains: "WillingnessEngine"
    - path: "plugins/core/src/index.ts"
      provides: "Root Config with nested willingness Schema"
      contains: "WillingnessSchema"
  key_links:
    - from: "plugins/core/src/services/agent/service.ts"
      to: "plugins/core/src/services/agent/willingness.ts"
      via: "new WillingnessEngine(config.willingness)"
      pattern: "new WillingnessEngine"
    - from: "plugins/core/src/services/agent/service.ts"
      to: "plugins/core/src/services/agent/willingness.ts"
      via: "engine.processMessage in gateAndEnqueue"
      pattern: "processMessage"
    - from: "plugins/core/src/index.ts"
      to: "plugins/core/src/services/agent/willingness-config.ts"
      via: "WillingnessSchema in root Schema"
      pattern: "WillingnessSchema"
---

<objective>
Wire the WillingnessEngine into AgentCore and the root plugin config, replacing all v1 willingness code and config fields.

Purpose: Complete the integration so the new algorithmic willingness system is active end-to-end — from config UI to gating decisions to fatigue tracking.

Output: Updated `config.ts`, `service.ts`, `index.ts` (agent), and `index.ts` (root) with old willingness code fully removed and new engine wired in.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-willingness-system-migration/10-CONTEXT.md
@.planning/phases/10-willingness-system-migration/10-01-SUMMARY.md
@plugins/core/src/services/agent/config.ts
@plugins/core/src/services/agent/service.ts
@plugins/core/src/services/agent/index.ts
@plugins/core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AgentCoreConfig and AgentCore service</name>
  <files>
    plugins/core/src/services/agent/config.ts
    plugins/core/src/services/agent/service.ts
    plugins/core/src/services/agent/index.ts
  </files>
  <action>
**config.ts changes:**
- Remove ALL old willingness fields: `willingnessModel`, `willingnessRejectThreshold`, `willingnessAcceptThreshold`, `willingCooldownMessages`, `willingCooldownMs`, `willingSoftDecayMs`
- Add `willingness: WillingnessConfig` field to `AgentCoreConfig`
- Import `WillingnessConfig` from `./willingness-config`

**service.ts changes:**
- Replace `import { WillingnessCalculator }` with `import { WillingnessEngine }` from `./willingness`
- Remove `import type { ModelService }` (no longer needed for willingness)
- Replace `private willingness = new WillingnessCalculator()` with `private willingness!: WillingnessEngine`
- In `start()`: instantiate engine `this.willingness = new WillingnessEngine(this.config.willingness)` and start decay timer `this.ctx.setInterval(() => this.willingness.tick(), 1000)`
- In `gateAndEnqueue()`:
  - Remove `this.willingness.incrementMessageCount(channelKey)` call
  - Remove `const modelService = ...` and the async `shouldReply` call
  - Replace with synchronous: `const { shouldReply } = this.willingness.processMessage(channelKey, (percept as UserMessagePercept).triggerType, (percept as UserMessagePercept).payload.content)`
  - Keep the rest of the gating logic (queue check, enqueue) unchanged
- In `runLoop()`: keep `this.willingness.recordReply(channelKey)` — method name is the same on WillingnessEngine

**index.ts (agent) changes:**
- Replace `export { WillingnessCalculator }` with `export { WillingnessEngine }` from `./willingness`
- Add `export type { WillingnessConfig }` from `./willingness-config`
  </action>
  <verify>TypeScript compiles: `cd /home/workspace/Athena && npx tsc --noEmit -p plugins/core/tsconfig.json 2>&1 | head -20`</verify>
  <done>AgentCore uses WillingnessEngine with synchronous processMessage; decay timer runs via ctx.setInterval; old willingness fields removed from AgentCoreConfig; agent index exports updated</done>
</task>

<task type="auto">
  <name>Task 2: Update root Config and Schema</name>
  <files>plugins/core/src/index.ts</files>
  <action>
**Root Config interface changes:**
- Remove: `willingnessModel`, `willingnessRejectThreshold`, `willingnessAcceptThreshold`, `willingCooldownMessages`, `willingCooldownMs`, `willingSoftDecayMs`
- Add: `willingness?: WillingnessConfig`
- Import `WillingnessConfig` type and `WillingnessSchema` from the agent willingness-config module

**Root Schema changes:**
- Remove all six old willingness Schema fields (`willingnessModel`, `willingnessRejectThreshold`, etc.)
- Add `willingness: WillingnessSchema` to the Schema.object (this creates a nested config group in Koishi UI)

**apply() function changes:**
- In the `ctx.plugin(AgentCore, {...})` call: remove the six old willingness field pass-throughs
- Add `willingness: config.willingness` (or spread defaults if undefined — but Schema defaults handle this)

**Build verification:**
- Run `yarn build` from repo root to confirm full build passes (typecheck + build via turbo)
  </action>
  <verify>`cd /home/workspace/Athena && yarn build 2>&1 | tail -20` — build succeeds with no errors</verify>
  <done>Root Config has nested `willingness` field with WillingnessSchema; old flat willingness fields fully removed; AgentCore receives willingness config; full build passes</done>
</task>

</tasks>

<verification>
- `yarn build` passes from repo root (typecheck + build for all packages)
- No references to `WillingnessCalculator` remain in codebase
- No references to old config fields (`willingnessModel`, `willingnessRejectThreshold`, etc.) remain
- `AgentCore.gateAndEnqueue` calls `processMessage` synchronously (no `await` on willingness check)
- `ctx.setInterval` used for decay timer (not raw `setInterval`)
- `recordBotReply` called after successful `runLoop`
</verification>

<success_criteria>
- Complete removal of v1 willingness system (WillingnessCalculator, LLM judge, old config fields)
- WillingnessEngine fully wired into AgentCore lifecycle (start → timer, percept → processMessage, reply → recordBotReply)
- Nested willingness config group visible in Koishi Schema
- Full monorepo build passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-willingness-system-migration/10-02-SUMMARY.md`
</output>
