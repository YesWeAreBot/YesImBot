---
phase: 01-foundation-shared-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - turbo.json
  - packages/shared-model/package.json
  - packages/shared-model/tsconfig.json
  - packages/shared-model/src/index.ts
  - packages/shared-model/src/types/model.ts
  - packages/shared-model/src/utils/model-id.ts
autonomous: true
requirements:
  - PLATFORM-01

must_haves:
  truths:
    - "shared-model package exports IModelProvider, IModel, ModelConfig types"
    - "shared-model package exports createModelId utility function"
    - "shared-model does NOT have ai-sdk as runtime dependency (devDependencies/peerDependencies only)"
    - "Root workspaces include packages/*, plugins/*, providers/*"
    - "turbo run build succeeds with shared-model producing dist output"
  artifacts:
    - path: "packages/shared-model/src/index.ts"
      provides: "Re-exports all types and utilities"
    - path: "packages/shared-model/src/types/model.ts"
      provides: "IModelProvider, ModelConfig, model-related types"
      contains: "IModelProvider"
    - path: "packages/shared-model/src/utils/model-id.ts"
      provides: "createModelId utility"
      contains: "createModelId"
    - path: "packages/shared-model/package.json"
      provides: "Package manifest with workspace-compatible config"
  key_links:
    - from: "packages/shared-model/src/index.ts"
      to: "packages/shared-model/src/types/model.ts"
      via: "re-export"
      pattern: "export.*from.*types/model"
    - from: "packages/shared-model/src/types/model.ts"
      to: "ai"
      via: "import type only"
      pattern: "import type.*from.*ai"
---

<objective>
Create the shared-model package with core model types and update root monorepo configuration to include the providers/* workspace directory.

Purpose: Establish the type foundation that all other packages (core plugin, provider plugins) depend on. The shared-model package defines the contract between core and providers.
Output: Buildable @yesimbot/shared-model package exporting IModelProvider, ModelConfig, and utility functions.
</objective>

<execution_context>
@C:/Users/MiaowFISH/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MiaowFISH/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shared-model/01-CONTEXT.md
@.planning/phases/01-foundation-shared-model/01-RESEARCH.md
@package.json
@tsconfig.base.json
@turbo.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update root workspaces and add providers directory placeholder</name>
  <files>package.json</files>
  <action>
Update root package.json workspaces array from `["packages/*", "plugins/*"]` to `["packages/*", "plugins/*", "providers/*"]` per locked decision (three top-level directories: packages/, plugins/, providers/).

Also add `"fmt"` and `"test"` tasks to turbo.json if not present — check existing turbo.json first. Current turbo.json already has fmt task via lint, so only add test if missing.

Create empty `providers/` directory with a `.gitkeep` file so the workspace entry doesn't cause errors before any provider packages exist.
  </action>
  <verify>
Run `cat package.json | grep -A5 workspaces` to confirm providers/* is listed.
Run `ls providers/.gitkeep` to confirm directory exists.
  </verify>
  <done>Root package.json workspaces includes "providers/*", providers/ directory exists.</done>
</task>

<task type="auto">
  <name>Task 2: Create @yesimbot/shared-model package with core types</name>
  <files>
    packages/shared-model/package.json
    packages/shared-model/tsconfig.json
    packages/shared-model/src/index.ts
    packages/shared-model/src/types/model.ts
    packages/shared-model/src/utils/model-id.ts
  </files>
  <action>
Create `packages/shared-model/` with the following structure:

**package.json**: Name `@yesimbot/shared-model`. Use pkgroll for build (consistent with root devDependencies pattern — pkgroll is already a root devDependency). Set `main`, `module`, `types` fields pointing to dist/. Add `ai` as devDependency (type-only usage — use `import type`). Add build/clean scripts matching turbo pipeline.

**tsconfig.json**: Extend `../../tsconfig.base.json`. Set `outDir: "./dist"`, `rootDir: "./src"`. Include `["src/**/*"]`.

**src/types/model.ts**: Define core model types:
- `import type { LanguageModelV1 } from 'ai'` (CRITICAL: import type only, no runtime dependency)
- `IModelProvider` interface: `{ readonly id: string; readonly name: string; getModel(modelId: string): LanguageModelV1 }`
- `ModelConfig` interface: `{ provider: string; model: string; temperature?: number; maxTokens?: number }`
- Re-export `LanguageModelV1` type from ai-sdk

**src/utils/model-id.ts**: Export `createModelId(provider: string, model: string): string` that returns `${provider}:${model}`.

**src/index.ts**: Re-export everything from `./types/model` and `./utils/model-id`.

IMPORTANT: shared-model must NOT have `ai` in `dependencies` — only in `devDependencies` (or `peerDependencies`). This prevents consumers from being forced to bundle ai-sdk runtime. Use `import type` exclusively for ai-sdk imports.
  </action>
  <verify>
Run `cd packages/shared-model && npx tsc --noEmit` to verify TypeScript compilation.
Run `turbo run build --filter=@yesimbot/shared-model` to verify build produces dist/ output.
Verify `ai` is NOT in dependencies (only devDependencies) in package.json.
  </verify>
  <done>
shared-model builds successfully, exports IModelProvider/ModelConfig/LanguageModelV1 types and createModelId utility, ai-sdk is type-only dependency.
  </done>
</task>

</tasks>

<verification>
1. `turbo run build --filter=@yesimbot/shared-model` succeeds
2. `packages/shared-model/dist/` contains compiled output
3. TypeScript types are properly exported (check .d.ts files in dist/)
4. `ai` package is NOT in shared-model's runtime dependencies
5. Root workspaces array includes all three directories
</verification>

<success_criteria>
- shared-model package builds and exports IModelProvider, ModelConfig, LanguageModelV1 types
- createModelId utility function is exported and functional
- Root monorepo recognizes all three workspace directories
- No runtime dependency on ai-sdk in shared-model
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-shared-model/01-01-SUMMARY.md`
</output>
