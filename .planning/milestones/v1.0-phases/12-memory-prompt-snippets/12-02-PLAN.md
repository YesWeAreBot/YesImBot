---
phase: 12-memory-prompt-snippets
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - plugins/core/src/services/memory/service.ts
  - plugins/core/src/index.ts
autonomous: true
requirements:
  - PROMPT-02

must_haves:
  truths:
    - "Built-in snippets supply current time in Chinese-friendly format to every rendered prompt"
    - "Built-in snippets supply sender nickname and ID to every rendered prompt"
    - "Built-in snippets supply channel name and platform to every rendered prompt"
    - "Built-in snippets supply bot name and ID to every rendered prompt"
    - "Memory config fields (coreMemoryPath, memoryCharLimit) appear in Koishi config UI"
  artifacts:
    - path: "plugins/core/src/services/memory/service.ts"
      provides: "Snippet registration in start()"
      contains: "registerSnippet"
    - path: "plugins/core/src/index.ts"
      provides: "MemoryService plugin wiring and Config/Schema fields"
      contains: "MemoryService"
  key_links:
    - from: "plugins/core/src/services/memory/service.ts"
      to: "plugins/core/src/services/prompt/service.ts"
      via: "registerSnippet() calls"
      pattern: "registerSnippet"
    - from: "plugins/core/src/index.ts"
      to: "plugins/core/src/services/memory/service.ts"
      via: "ctx.plugin(MemoryService, ...)"
      pattern: "ctx\\.plugin\\(MemoryService"
---

<objective>
Register built-in dynamic snippets and wire MemoryService into the core plugin config.

Purpose: Provide runtime context (time, sender, channel, bot) as template variables available in both memory blocks and system prompt templates, completing the prompt snippet system.
Output: 4 snippet types registered, MemoryService wired into core plugin with config UI fields.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-memory-prompt-snippets/12-RESEARCH.md
@.planning/phases/12-memory-prompt-snippets/12-CONTEXT.md
@.planning/phases/12-memory-prompt-snippets/12-01-SUMMARY.md
@plugins/core/src/services/memory/service.ts
@plugins/core/src/services/horizon/types.ts
@plugins/core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register built-in snippets in MemoryService</name>
  <files>
    plugins/core/src/services/memory/service.ts
  </files>
  <action>
In MemoryService.start(), after registerInjection(), add snippet registrations using `this.ctx["yesimbot.prompt"].registerSnippet(...)`.

Register these 4 snippet categories (natural language format, not structured key-value):

1. **date.now** — Chinese-friendly time format using `Intl.DateTimeFormat("zh-CN", { year: "numeric", month: "long", day: "numeric", weekday: "long", hour: "numeric", minute: "2-digit", hour12: true })`. Output example: "2026年2月20日 星期五 下午3:00".

2. **sender.name** and **sender.id** — Read from `scope.view` cast as `HorizonView`. sender.name: `view.percept.payload.sender.name ?? ""`. sender.id: `view.percept.payload.sender.id ?? ""`. Import `HorizonView` and `UserMessagePercept` types from the horizon types module.

3. **channel.name** and **channel.platform** — Read from `scope.view` cast as `HorizonView`. channel.name: `view.environment?.name ?? ""`. channel.platform: `(view.environment?.metadata?.platform as string) ?? ""`.

4. **bot.name** and **bot.id** — Read from `scope.view` cast as `HorizonView`. bot.name: `view.self?.name ?? ""`. bot.id: `view.self?.id ?? ""`.

Each snippet is a function `(scope) => string` that safely reads from the HorizonView in scope. Use optional chaining throughout to handle missing data gracefully (return empty string).

Import types at top of file:
```typescript
import type { HorizonView, UserMessagePercept } from "../horizon/types";
```
  </action>
  <verify>
Run `yarn typecheck` from repo root. All snippet registrations should compile without type errors.
  </verify>
  <done>
8 snippets registered (date.now, sender.name, sender.id, channel.name, channel.platform, bot.name, bot.id). All read from HorizonView scope with safe optional chaining. Time formatted in Chinese-friendly locale.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire MemoryService into core plugin Config and Schema</name>
  <files>
    plugins/core/src/index.ts
  </files>
  <action>
Update `plugins/core/src/index.ts`:

1. **Import:** Add `import { MemoryService, type MemoryConfig } from "./services/memory"`.

2. **Config interface:** Add `MemoryConfig` to the extends list: `export interface Config extends HorizonServiceConfig, PromptServiceConfig, PluginServiceConfig, ModelServiceConfig, MemoryConfig { ... }`.

3. **Schema:** Add two fields to the Schema.object:
   - `coreMemoryPath: Schema.path({ filters: ["directory"] }).description("Directory containing memory block files (.md/.txt)")` — uses Koishi's built-in directory picker
   - `memoryCharLimit: Schema.number().default(4000).description("Maximum characters for memory block injection")`

4. **apply():** Add `ctx.plugin(MemoryService, { coreMemoryPath: config.coreMemoryPath, memoryCharLimit: config.memoryCharLimit })` AFTER the PromptService plugin call (MemoryService depends on yesimbot.prompt).

5. **waitForServiceReady:** Add `"yesimbot.memory"` to the services array.

Key rules:
- MemoryService MUST be plugged after PromptService (dependency order)
- Schema.path with `filters: ["directory"]` provides a directory browser in Koishi console
- Config interface extends MemoryConfig to inherit coreMemoryPath and memoryCharLimit types
  </action>
  <verify>
Run `yarn typecheck` from repo root. Verify no type errors. Check that Config interface includes memory fields and Schema has the new entries.
  </verify>
  <done>
MemoryService is wired into the core plugin. Config UI shows directory picker for coreMemoryPath and number input for memoryCharLimit. MemoryService starts after PromptService and is included in service readiness check.
  </done>
</task>

</tasks>

<verification>
1. `yarn typecheck` passes with no errors
2. All 8 snippets (date.now, sender.name, sender.id, channel.name, channel.platform, bot.name, bot.id) are registered
3. MemoryService appears in core plugin's apply() after PromptService
4. Config interface extends MemoryConfig
5. Schema includes coreMemoryPath (directory picker) and memoryCharLimit fields
6. waitForServiceReady includes yesimbot.memory
</verification>

<success_criteria>
- Built-in snippets resolve from HorizonView scope with safe optional chaining
- Time snippet outputs Chinese-friendly format via Intl.DateTimeFormat
- MemoryService wired into core plugin with proper dependency ordering
- Config UI exposes memory directory path and character limit settings
- Full typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/12-memory-prompt-snippets/12-02-SUMMARY.md`
</output>
