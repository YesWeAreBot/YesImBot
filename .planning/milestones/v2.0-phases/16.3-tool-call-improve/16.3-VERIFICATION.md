---
phase: 16.3-tool-call-improve
verified: 2026-02-22T09:35:25Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase 16.3: tool-call-improve Verification Report

**Phase Goal:** Replace native AI SDK tool_call with structured JSON text output, manual heartbeat loop, and prompt-based tool schema rendering
**Verified:** 2026-02-22T09:35:25Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | ThinkActLoop uses a manual heartbeat loop — no tools/toolChoice/stopWhen passed to AI SDK | ✓ VERIFIED | `loop.ts:82-91` — `CallParams` has only `system` + `messages`; grep confirms zero occurrences of `toolChoice`, `stopWhen`, `onStepFinish` |
| 2 | Model output is parsed as JSON with AgentResponse schema (actions array + optional thoughts + request_heartbeat) | ✓ VERIFIED | `loop.ts:108` — `parser.parse(rawText)`; `AgentResponse` interface at lines 13-17 |
| 3 | Tool-type actions execute in parallel, Action-type actions execute sequentially | ✓ VERIFIED | `loop.ts:225-253` — `Promise.allSettled` for Tool-type, sequential `for` loop for Action-type |
| 4 | Loop auto-continues when Tool calls present, auto-stops when only Action calls, with request_heartbeat override | ✓ VERIFIED | `loop.ts:143-146` — `autoShouldContinue = hasToolCalls && !hasActionCalls`; `shouldContinue = response.request_heartbeat ?? autoShouldContinue` |
| 5 | Max rounds limit triggers a forced wrap-up round before termination | ✓ VERIFIED | `loop.ts:149-173` — `if (round >= maxRounds)` appends wrap-up prompt and executes final call |
| 6 | Tool results are appended as user messages (assistant JSON + user tool-results pairs) | ✓ VERIFIED | `loop.ts:176-177` — `messages.push({ role: "assistant", content: rawText })` + `messages.push({ role: "user", content: formatToolResults(toolResults) })` |
| 7 | send_message is the only reply channel — no fallback direct text sending | ✓ VERIFIED | No `session.send` or direct reply call in `loop.ts`; only `pluginService.invoke` used for output |
| 8 | basic_functions is NOT in CACHEABLE_POINTS (tool list is dynamic per request) | ✓ VERIFIED | `prompt/service.ts:28-33` — `CACHEABLE_POINTS` contains only `identity`, `style`, `control_flow`, `memory` |
| 9 | System prompt control_flow and basic_functions templates describe JSON output format and tool schemas | ✓ VERIFIED | `default-control-flow.md` — full JSON schema with `actions`, `thoughts`, `request_heartbeat`; `default-basic-functions.md` — tool/action distinction |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/src/services/agent/json-parser.ts` | JsonParser with jsonrepair fallback | ✓ VERIFIED | 151 lines; exports `JsonParser<T>`, `ParseResult<T>`, `Logger`; jsonrepair imported and used |
| `core/src/services/agent/tools.ts` | buildToolSchemaForPrompt for prompt injection | ✓ VERIFIED | 16 lines; exports only `buildToolSchemaForPrompt`; calls `pluginService.getTools()` |
| `core/src/services/agent/loop.ts` | Manual heartbeat loop with JSON parsing | ✓ VERIFIED | 327 lines; `ThinkActLoop.run()` with while loop, `parseAgentResponse` via `JsonParser`, `executeActions` partitioning |
| `core/resources/templates/default-control-flow.md` | JSON output format instructions | ✓ VERIFIED | Contains `actions`, `thoughts`, `request_heartbeat` schema; explains continuation rules |
| `core/resources/templates/default-basic-functions.md` | Tool/action distinction instructions | ✓ VERIFIED | Explains tool vs action semantics; references `actions` array |
| `core/src/services/prompt/service.ts` | basic_functions removed from CACHEABLE_POINTS | ✓ VERIFIED | CACHEABLE_POINTS set has 4 entries, no `basic_functions` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `loop.ts` | `json-parser.ts` | `import JsonParser` | ✓ WIRED | `loop.ts:9` — `import { JsonParser, type ParseResult } from "./json-parser"` |
| `loop.ts` | `tools.ts` | `import buildToolSchemaForPrompt` | ✓ WIRED | `loop.ts:10` — `import { buildToolSchemaForPrompt } from "./tools"` |
| `loop.ts` | `plugin/service.ts` | `pluginService.invoke()` | ✓ WIRED | `loop.ts:228,243` — `pluginService.invoke(action.name, action.params ?? {}, toolCtx)` |
| `json-parser.ts` | `jsonrepair` | `import { jsonrepair }` | ✓ WIRED | `json-parser.ts:1` — `import { jsonrepair, JSONRepairError } from "jsonrepair"` |
| `tools.ts` | `plugin/service.ts` | `pluginService.getTools()` | ✓ WIRED | `tools.ts:8` — `pluginService.getTools(toolCtx)` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PHASE-16.3 | 16.3-02-PLAN.md | Replace AI SDK tool_call with structured JSON + manual heartbeat loop | ✓ SATISFIED | All 9 truths verified; build passes; no AI SDK tool params in loop |

Note: `PHASE-16.3` is a phase-level requirement ID used internally in the plans. It does not appear in `REQUIREMENTS.md` (which tracks v2.0 architectural requirements mapped to phases 16-19). No orphaned requirements found.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| — | — | None found | — | — |

No TODO/FIXME/placeholder comments, empty implementations, or stub patterns detected in any modified file.

### Human Verification Required

None. All observable truths are verifiable programmatically for this phase.

### Gaps Summary

No gaps. All must-haves from both plans are satisfied:

- `json-parser.ts` is substantive (151 lines, handles clean/markdown-wrapped/malformed JSON with jsonrepair fallback)
- `tools.ts` exports only `buildToolSchemaForPrompt`, all AI SDK wrappers removed
- `loop.ts` is a complete manual heartbeat loop — no AI SDK tool params, JSON parsed via `JsonParser`, actions partitioned by `FunctionType`, continuation inferred from types with `request_heartbeat` override, max rounds triggers wrap-up
- Templates instruct the model on JSON output format and tool/action semantics
- `basic_functions` removed from `CACHEABLE_POINTS`
- Build passes cleanly (turbo cache hit, 4/4 packages successful)

---

_Verified: 2026-02-22T09:35:25Z_
_Verifier: Claude (gsd-verifier)_
