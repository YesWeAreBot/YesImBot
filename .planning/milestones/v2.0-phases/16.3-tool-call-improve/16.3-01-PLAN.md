---
phase: 16.3-tool-call-improve
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/package.json
  - core/src/services/agent/json-parser.ts
  - core/src/services/agent/tools.ts
autonomous: true
requirements:
  - PHASE-16.3
must_haves:
  truths:
    - "JsonParser can parse clean JSON, markdown-wrapped JSON, and malformed JSON (via jsonrepair) into typed objects"
    - "buildToolSchemaForPrompt() renders all available tools as structured text suitable for system prompt injection"
    - "finishTool, buildAiSdkTools, and buildStopCondition are removed from tools.ts"
  artifacts:
    - path: "core/src/services/agent/json-parser.ts"
      provides: "Robust JSON parser ported from v3 with jsonrepair fallback"
      contains: "class JsonParser"
    - path: "core/src/services/agent/tools.ts"
      provides: "Tool schema text builder for prompt injection"
      exports: ["buildToolSchemaForPrompt"]
  key_links:
    - from: "core/src/services/agent/json-parser.ts"
      to: "jsonrepair"
      via: "import { jsonrepair } from 'jsonrepair'"
      pattern: "jsonrepair"
    - from: "core/src/services/agent/tools.ts"
      to: "core/src/services/plugin/service.ts"
      via: "pluginService.getTools()"
      pattern: "getTools"
---

<objective>
Create the foundation components for the structured JSON tool-call architecture: a robust JSON parser (ported from v3) and a prompt-based tool schema builder replacing the AI SDK tool wrappers.

Purpose: These are the building blocks consumed by the ThinkActLoop rewrite in Plan 02.
Output: `json-parser.ts` (new file), rewritten `tools.ts`, `jsonrepair` dependency added.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16.3-tool-call-improve/16.3-RESEARCH.md

@references/YesImBot-v3/packages/core/src/shared/utils/json-parser.ts
@core/src/services/agent/tools.ts
@core/src/services/plugin/types.ts
@core/src/services/plugin/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add jsonrepair dependency and create JsonParser</name>
  <files>
    core/package.json
    core/src/services/agent/json-parser.ts
  </files>
  <action>
1. Run `cd core && yarn add jsonrepair` to add the dependency.

2. Create `core/src/services/agent/json-parser.ts` — port from v3's `references/YesImBot-v3/packages/core/src/shared/utils/json-parser.ts` with these adaptations:
   - Keep the same core logic: markdown code block stripping, JSON start/end detection, balanced bracket check, jsonrepair fallback, isLikelyJsonStart heuristic
   - Remove v3's Chinese log messages — use English
   - Use a simple `Logger` interface (just `info` method) instead of importing Koishi's Logger directly, so the parser stays unit-testable
   - Generic type parameter `<T>` on the class
   - Export `ParseResult<T>` interface and `JsonParser<T>` class
   - Keep the parser stateless per-call (logs array reset each parse call, matching v3 behavior)
   - Do NOT add streaming support (deferred per RESEARCH.md open question #1)
  </action>
  <verify>
    `cd /home/workspace/Athena && yarn build` succeeds (typecheck + build).
    Verify `jsonrepair` appears in `core/package.json` dependencies.
  </verify>
  <done>
    `json-parser.ts` exists with `JsonParser<T>` class that handles: clean JSON, markdown-wrapped JSON, malformed JSON via jsonrepair, and the isLikelyJsonStart heuristic to avoid `[OBSERVE]` false positives.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite tools.ts — remove AI SDK wrappers, add prompt schema builder</name>
  <files>
    core/src/services/agent/tools.ts
  </files>
  <action>
1. Remove all AI SDK imports (`hasToolCall`, `jsonSchema`, `stepCountIs`, `ToolSet`).
2. Remove `finishTool`, `buildAiSdkTools()`, and `buildStopCondition()`.
3. Add `buildToolSchemaForPrompt(pluginService, toolCtx)` function:
   - Calls `pluginService.getTools(toolCtx)` to get available tool entries
   - For each entry, render a compact text block:
     ```
     - tool_name (tool|action): description
       Parameters: { JSON schema object }
     ```
   - The `(tool|action)` label comes from `entry.functionType` (FunctionType.Tool → "tool", FunctionType.Action → "action")
   - Parameters rendered via `JSON.stringify(entry.function.parameters)` (already JSON Schema from `schemaToJSONSchema`)
   - Return the joined string
4. Export only `buildToolSchemaForPrompt`.
  </action>
  <verify>
    `cd /home/workspace/Athena && yarn build` succeeds.
    Grep confirms no remaining references to `finishTool`, `buildAiSdkTools`, `buildStopCondition` in `tools.ts`.
  </verify>
  <done>
    `tools.ts` exports only `buildToolSchemaForPrompt()`. All AI SDK tool-related code is removed. The function produces human-readable tool schema text from PluginService entries.
  </done>
</task>

</tasks>

<verification>
1. `yarn build` passes from repo root (typecheck + build for all packages)
2. `core/src/services/agent/json-parser.ts` exists and exports `JsonParser`
3. `core/src/services/agent/tools.ts` exports only `buildToolSchemaForPrompt`
4. No remaining imports of `hasToolCall`, `jsonSchema`, `stepCountIs` in agent directory
5. `jsonrepair` is in `core/package.json` dependencies
</verification>

<success_criteria>
- JsonParser class handles clean, markdown-wrapped, and malformed JSON with jsonrepair fallback
- buildToolSchemaForPrompt renders tool definitions as structured text for prompt injection
- All AI SDK tool-calling artifacts removed from tools.ts
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/16.3-tool-call-improve/16.3-01-SUMMARY.md`
</output>
