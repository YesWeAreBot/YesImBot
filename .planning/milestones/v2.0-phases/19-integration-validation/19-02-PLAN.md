---
phase: 19-integration-validation
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - core/resources/skills/private-chat/SKILL.md
  - core/resources/skills/image-gen/SKILL.md
  - core/resources/skills/image-gen/scripts/activate.js
  - core/resources/skills/mention-aware/SKILL.md
autonomous: true
requirements: [SKILL-05]

must_haves:
  truths:
    - "private-chat skill activates on scene:private-chat signal and applies style effect"
    - "image-gen skill uses code activator with keyword matching against triggerContent metadata"
    - "mention-aware skill activates on attention:mentioned signal and injects prompt guidance"
    - "Three example skills cover all three effect types: style, tools, prompt"
  artifacts:
    - path: "core/resources/skills/private-chat/SKILL.md"
      provides: "Style effect skill triggered by scene:private-chat"
      contains: "scene"
    - path: "core/resources/skills/image-gen/scripts/activate.js"
      provides: "Code activator with keyword matching"
      contains: "triggerContent"
    - path: "core/resources/skills/mention-aware/SKILL.md"
      provides: "Prompt effect skill triggered by attention:mentioned"
      contains: "attention"
  key_links:
    - from: "core/resources/skills/private-chat/SKILL.md"
      to: "core/src/services/trait/detectors/scene.ts"
      via: "scene:private-chat signal matching"
      pattern: "dimension: scene"
    - from: "core/resources/skills/image-gen/scripts/activate.js"
      to: "core/src/services/trait/detectors/scene.ts"
      via: "triggerContent metadata from scene signal"
      pattern: "triggerContent"
    - from: "core/resources/skills/mention-aware/SKILL.md"
      to: "core/src/services/trait/detectors/scene.ts"
      via: "attention:mentioned signal matching"
      pattern: "dimension: attention"
---

<objective>
Create and fix three example skills that validate all three effect types (style, tools, prompt) through the Trait-Skill pipeline wired in Plan 01.

Purpose: Prove the end-to-end Trait→Skill→Effect system works with real skill definitions covering all effect types.
Output: Three working skill folders in core/resources/skills/ demonstrating style, tools, and prompt effects.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-integration-validation/19-RESEARCH.md
@.planning/phases/19-integration-validation/19-01-PLAN.md
@core/src/services/skill/loader.ts
@core/src/services/skill/condition.ts
@core/src/services/trait/detectors/scene.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix private-chat skill conditions and rewrite image-gen with code activator</name>
  <files>
    core/resources/skills/private-chat/SKILL.md
    core/resources/skills/image-gen/SKILL.md
    core/resources/skills/image-gen/scripts/activate.js
  </files>
  <action>
**private-chat/SKILL.md** — Fix condition to match actual SceneTrait signal:
- Current condition uses `dimension: scope, value: isDirect` — this is wrong
- SceneTrait emits `dimension: scene, value: private-chat` (see scene.ts line 66)
- Change to: `match: { dimension: scene, value: private-chat }`
- Keep lifecycle as `trait-bound`, keep style effect content unchanged

**image-gen/SKILL.md** — Remove YAML conditions (replaced by code activator):
- Remove the `conditions:` block entirely (the `or:` with intent matches)
- The code activator in scripts/activate.js will handle activation
- Keep lifecycle as `sticky`, keep stickyTimeout as 3, keep tools effect unchanged
- Keep the prompt content (after `---`) as-is

**image-gen/scripts/activate.js** — Create code activator with keyword matching:
- Per CONTEXT.md: check for keywords like 画画, draw, etc.
- The activator receives `signals` (TraitSignal[]) — check `metadata.triggerContent` on each signal
- Keywords: `['画', '绘', 'draw', 'paint', 'sketch', 'generate image', '生成图', '图片']`
- Return true if any signal's triggerContent contains any keyword (case-insensitive for Latin chars)
- Export as `module.exports = function activate(signals) { ... }`
  </action>
  <verify>Verify files exist: `ls core/resources/skills/private-chat/SKILL.md core/resources/skills/image-gen/SKILL.md core/resources/skills/image-gen/scripts/activate.js`. Run `yarn typecheck` and `yarn build` to confirm no regressions.</verify>
  <done>private-chat matches scene:private-chat signal, image-gen uses code activator with keyword matching against triggerContent metadata, both skills load without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create mention-aware skill with prompt effect</name>
  <files>core/resources/skills/mention-aware/SKILL.md</files>
  <action>
Create `core/resources/skills/mention-aware/SKILL.md` — a prompt-effect skill:
- YAML frontmatter:
  - name: mention-aware
  - description: Respond attentively when directly mentioned
  - conditions: `match: { dimension: attention, value: mentioned }`
  - lifecycle: per-turn
  - effects: {} (empty — prompt content comes from markdown body)
- Markdown body (after `---`): prompt guidance text telling the bot to pay attention and respond directly to the user who mentioned them. Keep it concise (2-3 sentences in Chinese, matching the project's language style).

The loader (loader.ts) assigns markdown body content to `effects.prompt`, and mergeEffects wraps it in `<skill name="mention-aware">...</skill>` XML tags injected at the "extra" point.
  </action>
  <verify>Verify file exists: `ls core/resources/skills/mention-aware/SKILL.md`. Run `yarn build` to confirm the full build passes with all three skills loadable.</verify>
  <done>mention-aware skill folder exists with SKILL.md, activates on attention:mentioned signal, injects prompt guidance as extra injection. Three skills cover all three effect types: private-chat (style), image-gen (tools), mention-aware (prompt).</done>
</task>

</tasks>

<verification>
1. `yarn build` passes
2. Three skill folders exist under core/resources/skills/: private-chat, image-gen, mention-aware
3. private-chat condition matches `scene:private-chat` (not `scope:isDirect`)
4. image-gen has scripts/activate.js that checks triggerContent for drawing keywords
5. mention-aware condition matches `attention:mentioned`
6. Each skill demonstrates a different effect type: style, tools, prompt
</verification>

<success_criteria>
- Three example skills load successfully and cover all three effect types
- private-chat activates in DM conversations via scene:private-chat signal
- image-gen activates via code activator keyword matching on triggerContent
- mention-aware activates when bot is @-mentioned via attention:mentioned signal
- typecheck and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-integration-validation/19-02-SUMMARY.md`
</output>
