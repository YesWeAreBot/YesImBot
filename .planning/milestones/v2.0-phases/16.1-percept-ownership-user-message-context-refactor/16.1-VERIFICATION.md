---
phase: 16.1-percept-ownership-user-message-context-refactor
verified: 2026-02-21T13:00:00Z
status: passed
score: 11/11 must-haves verified
re_verification: false
---

# Phase 16.1: Percept Ownership & User Message Context Refactor — Verification Report

**Phase Goal:** Percept 构造从 horizon 移到 agent 模块，horizon 只负责数据记录和事件广播；user message 承载全部工作负载（环境、成员、历史），system prompt 变为纯静态；统一使用模板路径渲染上下文
**Verified:** 2026-02-21T13:00:00Z
**Status:** PASSED
**Re-verification:** No — initial verification

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | horizon/listener broadcasts `horizon/message` raw events instead of constructing Percept | VERIFIED | `listener.ts:32` — `this.ctx.emit("horizon/message", {...})` after `recordUserMessage`; no `buildPercept`/`schedulePercept` present |
| 2 | agent module subscribes to `horizon/message`, computes willingness per-event, manages aggregation window | VERIFIED | `service.ts:103` — `this.ctx.on("horizon/message", (event) => this.handleEvent(event))`; `handleEvent` calls `willingness.processMessage` and manages `pendingWindows` |
| 3 | Percept is constructed in agent module from last event in window, not in horizon | VERIFIED | `service.ts:155` — `buildPercept(event: HorizonMessageEvent): UserMessagePercept` in `AgentCore`; no Percept construction in horizon |
| 4 | Direct messages bypass aggregation window and trigger immediate Percept construction | VERIFIED | `service.ts:128-135` — `if (event.scope.isDirect)` branch calls `buildPercept` and `enqueue` immediately |
| 5 | Aggregation window duration is configurable via AgentCoreConfig | VERIFIED | `service.ts:53,67` — `aggregationWindow?: number` in interface; `Schema.number().default(1500)` in schema |
| 6 | System prompt is purely static (identity + style + memories), no dynamic environment data | VERIFIED | `system.mustache` has only `{{>identity}}`, `{{>style}}`, `{{>core_memories}}`, `{{>working_memory}}`, `{{>extra}}`; no `{{>environment}}` |
| 7 | User message contains full context (environment, members, history, trigger) rendered via horizon-view.mustache | VERIFIED | `loop.ts:43` — `const userContent = horizon.formatHorizonText(view)`; `messages = [{ role: "user", content: userContent }]` |
| 8 | History section contains only Active-stage messages; trigger section contains New-stage messages | VERIFIED | `service.ts:248` — `if (obs.type === "message" && obs.stage === "new") triggerObs.push(...)` else `historyObs.push(...)`; stage propagated via `manager.ts:84` |
| 9 | formatHorizonText() is the sole context rendering path; toStructured() and manual envLines are deleted | VERIFIED | No `toStructured`, `envLines`, `environment_content`, or `has_environment` anywhere in codebase; `formatHorizonText` called in `loop.ts:43` and `service.ts:242` (deferred judgment) |
| 10 | Deferred judgment continues working with the unified template path | VERIFIED | `service.ts:242` — `executeDeferredJudgment` calls `horizon.formatHorizonText(view)` for context |
| 11 | Empty sections show placeholder text so LLM knows the dimension exists | VERIFIED | `horizon-view.mustache` — `{{^environment}}No environment info available.{{/environment}}`, `{{^activeMembers}}No member info available.{{/activeMembers}}`, `{{^hasHistory}}No recent messages.{{/hasHistory}}`, `{{^hasTrigger}}No new messages.{{/hasTrigger}}` |

**Score:** 11/11 truths verified

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/src/services/horizon/types.ts` | HorizonMessageEvent, HorizonEventMap, BasePerceptRef; Percept types removed | VERIFIED | All three interfaces present; no PerceptType/UserMessagePercept/StructuredHorizonView |
| `core/src/services/horizon/listener.ts` | Raw event broadcast via ctx.emit('horizon/message') | VERIFIED | Emits at line 32; no buildPercept/schedulePercept/pendingPercepts |
| `core/src/services/agent/service.ts` | Percept types, handleEvent, buildPercept, aggregation window | VERIFIED | All present; PerceptType enum, UserMessagePercept, handleEvent, buildPercept, pendingWindows |
| `core/resources/templates/partials/horizon-view.mustache` | XML section tags with history/trigger split and placeholders | VERIFIED | Four XML sections with mustache conditionals and placeholder text |
| `core/resources/templates/system.mustache` | Static template without environment partial | VERIFIED | Five partials only; no {{>environment}} |
| `core/src/services/horizon/service.ts` | formatHorizonText with stage split; no toStructured() | VERIFIED | Stage split at line 248; toStructured absent |
| `core/src/services/horizon/manager.ts` | stage propagated in toObservations() | VERIFIED | `stage: entry.stage` at line 84 |
| `core/src/services/prompt/service.ts` | environment removed from partialMap | VERIFIED | partialMap has 5 entries (identity, style, core_memories, working_memory, extra); no environment key |
| `core/resources/templates/partials/environment.mustache` | Emptied (blank) | VERIFIED | File contains single blank line |
| `core/src/services/agent/loop.ts` | Uses formatHorizonText for user message; no dual rendering path | VERIFIED | Line 43: `formatHorizonText(view)`; no toStructured/envLines references |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `horizon/listener.ts` | `agent/service.ts` | `ctx.emit('horizon/message')` → `ctx.on('horizon/message')` | WIRED | listener.ts:32 emits; service.ts:103 subscribes |
| `agent/loop.ts` | `horizon/service.ts` | `horizon.formatHorizonText(view)` for user message content | WIRED | loop.ts:43 calls formatHorizonText; result used as `userContent` in messages array |
| `horizon-view.mustache` | `horizon/service.ts` | Mustache.render with observation stage split | WIRED | service.ts:255 — `Mustache.render(this.horizonViewTpl, { environment, activeMembers, hasHistory, history, hasTrigger, trigger })` |
| `agent/service.ts` (deferred) | `horizon/service.ts` | `horizon.formatHorizonText(view)` | WIRED | service.ts:242 — deferred judgment uses same rendering path |

---

### Requirements Coverage

No requirement IDs declared in either plan's frontmatter (`requirements: []`). This is a gap-closure/refactor phase with no REQUIREMENTS.md entries to cross-reference.

---

### Anti-Patterns Found

None. No TODO/FIXME/placeholder comments, no stub implementations, no empty handlers found in modified files.

---

### Human Verification Required

None — all goal truths are verifiable programmatically via code inspection.

---

## Gaps Summary

No gaps. All 11 observable truths verified against actual code. The phase goal is fully achieved:

- Horizon is a pure data layer: records timeline entries, broadcasts `horizon/message` raw events
- Agent owns all decision logic: willingness gating, configurable aggregation window (default 1500ms), direct-message bypass, Percept construction
- Single rendering path: `formatHorizonText()` → `horizon-view.mustache` used by both loop and deferred judgment
- System prompt is static (identity/style/memories only)
- User message carries full dynamic context via XML-sectioned template with empty-section placeholders

---

_Verified: 2026-02-21T13:00:00Z_
_Verifier: Claude (gsd-verifier)_
