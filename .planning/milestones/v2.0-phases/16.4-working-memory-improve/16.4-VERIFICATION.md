---
phase: 16.4-working-memory-improve
verified: 2026-02-22T13:11:32Z
status: passed
score: 9/9 must-haves verified
re_verification: false
---

# Phase 16.4: Working Memory Improve — Verification Report

**Phase Goal:** Transform ThinkActLoop messages array from unbounded growth into budget-controlled working memory with progressive trimming, replace post-loop AgentSummary with per-round AgentResponse timeline recording, and present layered context (history + working-memory) to the LLM
**Verified:** 2026-02-22T13:11:32Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | trimMessages() progressively degrades older rounds (softTrim then hardClear) while preserving last N rounds and index 0 | VERIFIED | `trimmer.ts` lines 37-75: two-pass loop, `eligibleEnd` excludes index 0 and last `keepLastRounds` rounds, `_trimState` tracks degradation |
| 2 | AgentSummary type is fully replaced by AgentResponse across timeline types, manager, and observation pipeline | VERIFIED | `types.ts`: `AgentResponse = "agent.response"`, `AgentResponseData`, `AgentResponseRecord`, `AgentResponseObservation`. Zero grep matches for `AgentSummary` or `agent.summary` in `core/src/` |
| 3 | History formatting shows agent action summary lines (tool name list) instead of raw summary strings | VERIFIED | `service.ts` lines 213-221: `formatObservation` for `agent.response` builds tool name list from `obs.data.actions` |
| 4 | send_message actions show full sent content in history summary line | VERIFIED | `service.ts` lines 214-219: `sendAction.params?.content` shown inline as `[Bot]: {content}`, other tools appended as `[also: ...]` |
| 5 | Each loop round records an AgentResponse event to timeline immediately after tool execution, before any trimming | VERIFIED | `loop.ts` lines 175-184: `recordAgentResponse` called after `executeActions`, before `shouldContinue` check |
| 6 | trimMessages() is called before each modelService.call() to enforce character budget | VERIFIED | `loop.ts` line 103: before main loop call; line 197: before wrap-up call — 2 call sites confirmed |
| 7 | The LLM receives a working-memory section in the user message showing tool execution details from recent rounds | VERIFIED | `loop.ts` lines 64-78: extracts `agent.response` observations from `view.history`, builds `wmLines`, passes to `formatHorizonText(view, wmLines)` |
| 8 | History and working-memory present two complementary perspectives explained in the prompt | VERIFIED | `horizon-view.mustache` lines 42-48: `<working-memory>` section with explanatory note distinguishing history (conversation flow) from working-memory (tool execution details) |
| 9 | Wrap-up round also applies trimming before its final LLM call | VERIFIED | `loop.ts` lines 197-203: `trimMessages(messages, trimConfig)` called before wrap-up `modelService.call()`, and wrap-up `recordAgentResponse` at lines 213-223 |

**Score:** 9/9 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `core/src/services/agent/trimmer.ts` | trimMessages pure function with softTrim/hardClear helpers | VERIFIED | Exports `trimMessages`, `TrimConfig`, `LoopMessage`; internal `softTrim` and `hardClearToolResult` helpers present |
| `core/src/services/horizon/types.ts` | AgentResponseData type replacing AgentSummaryData | VERIFIED | `AgentResponseData`, `AgentResponseRecord`, `AgentResponseObservation`, `Observation` union all present; no AgentSummary remnants |
| `core/src/services/horizon/manager.ts` | recordAgentResponse method replacing recordAgentSummary | VERIFIED | `recordAgentResponse` at lines 59-74; `toObservations` returns `agent.response` branch at lines 88-93 |
| `core/src/services/horizon/service.ts` | Updated formatObservation for agent.response observations | VERIFIED | `formatObservation` handles `agent.response` at lines 213-221; `formatHorizonText` accepts optional `workingMemory?: string[]` at line 226 |
| `core/src/services/agent/loop.ts` | Per-round AgentResponse recording, trimMessages integration, working-memory data flow | VERIFIED | All three concerns present and wired |
| `core/src/services/agent/service.ts` | TrimConfig fields in AgentCoreConfig | VERIFIED | `charBudget`, `keepLastRounds`, `softTrimHead`, `softTrimTail` in both interface and Schema with defaults |
| `core/resources/templates/partials/horizon-view.mustache` | working-memory section template | VERIFIED | `{{#hasWorkingMemory}}` block with `<working-memory>` tag and explanatory note |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `manager.ts` | `types.ts` | `AgentResponseRecord` type import | WIRED | Line 6: `import type { AgentResponseData, AgentResponseRecord, ... }` |
| `service.ts` | `types.ts` | `Observation` union with `AgentResponseObservation` | WIRED | `formatObservation` dispatches on `obs.type === "agent.response"` (line 213); `buildView` queries `TimelineEventType.AgentResponse` (line 113) |
| `loop.ts` | `trimmer.ts` | `trimMessages` import and call before `modelService.call` | WIRED | Line 12 import; lines 103 and 197 call sites |
| `loop.ts` | `manager.ts` | `recordAgentResponse` call after `executeActions` | WIRED | Lines 175-184 (main round); lines 213-223 (wrap-up round) |
| `loop.ts` | `horizon-view.mustache` | `workingMemory` data passed to `formatHorizonText` | WIRED | Lines 64-78 extract `wmLines`; line 78 passes to `formatHorizonText(view, wmLines)` |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| PHASE-16.4 | 16.4-01-PLAN.md, 16.4-02-PLAN.md | Working memory improvement (trimming + per-round recording + layered context) | SATISFIED | All 9 truths verified; typecheck passes (4/4 packages cached clean) |

Note: `PHASE-16.4` is an internal phase-scoped requirement ID, not tracked in `REQUIREMENTS.md` traceability table. No orphaned REQUIREMENTS.md entries map to Phase 16.4.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `horizon/service.ts` | 131, 177 | `return null` / `return []` | Info | Legitimate guard clauses (`!scope.channelId`, `!parentId`), not stubs |

No blockers or warnings found.

### Human Verification Required

None — all goal truths are verifiable programmatically for this phase.

### Gaps Summary

No gaps. All must-haves from both plans are fully implemented and wired:

- `trimmer.ts` implements two-level progressive degradation with correct round identification and `_trimState` tracking
- AgentSummary is completely eliminated from the codebase (zero grep matches)
- `formatObservation` renders meaningful action summary lines with send_message content inline
- `trimMessages` is called at both LLM call sites (main loop + wrap-up)
- `recordAgentResponse` fires per-round immediately after tool execution, including wrap-up
- Working-memory section in mustache template with explanatory note
- Config fields with Schema defaults wired into `AgentCoreConfig`
- Typecheck passes clean (4/4 packages)

---

_Verified: 2026-02-22T13:11:32Z_
_Verifier: Claude (gsd-verifier)_
