---
phase: 23-bug-fixes-reliability
plan: 01
type: execute
wave: 2
depends_on: [23-00]
files_modified:
  - core/package.json
  - core/src/services/agent/__tests__/json-parser.test.ts
autonomous: true
requirements:
  - BUGFIX-02

must_haves:
  truths:
    - "JSON parser test suite runs via vitest and passes all 18 cases ported from v3"
    - "vitest is installed as devDependency in core workspace"
    - "Test script exists in core/package.json"
  artifacts:
    - path: "core/src/services/agent/__tests__/json-parser.test.ts"
      provides: "18 test cases covering perfect JSON, code blocks, nested code blocks, OBSERVE prefix, truncated strings, dangling keys"
      contains: "describe"
    - path: "core/package.json"
      provides: "vitest devDependency and test script"
      contains: "vitest"
  key_links:
    - from: "core/src/services/agent/__tests__/json-parser.test.ts"
      to: "core/src/services/agent/json-parser.ts"
      via: "import { JsonParser }"
      pattern: "import.*JsonParser.*json-parser"
---

<objective>
Install vitest and port the v3 JSON parser test suite (18 cases) to vitest for the v4 codebase.

Purpose: Establish test coverage for the JSON parser, preventing silent regressions in LLM output parsing — the most fragile part of the agent loop.
Output: Working vitest test suite with 18 passing cases.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-bug-fixes-reliability/23-RESEARCH.md
@references/YesImBot-v3/packages/core/tests/utils-json-parser.test.ts
@core/src/services/agent/json-parser.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install vitest and add test script</name>
  <files>core/package.json</files>
  <action>
Run `yarn workspace koishi-plugin-yesimbot add -D vitest` to install vitest.

Then add `"test": "vitest run"` to the `scripts` section of `core/package.json`.

Do NOT add a vitest config file — vitest auto-discovers `**/*.test.ts` files without one.
  </action>
  <verify>
    <automated>yarn workspace koishi-plugin-yesimbot exec vitest --version</automated>
    <manual>vitest binary is resolvable and prints a version number</manual>
  </verify>
  <done>vitest is installed, `yarn workspace koishi-plugin-yesimbot test` runs without error</done>
</task>

<task type="auto">
  <name>Task 2: Port v3 JSON parser test suite to vitest</name>
  <files>core/src/services/agent/__tests__/json-parser.test.ts</files>
  <action>
Create `core/src/services/agent/__tests__/json-parser.test.ts` by porting the 18 test cases from `references/YesImBot-v3/packages/core/tests/utils-json-parser.test.ts`.

Key adaptations:
1. Replace `import { describe, it, expect } from "bun:test"` with `import { describe, it, expect } from "vitest"`
2. Import from the v4 parser: `import { JsonParser } from "../json-parser"`
3. Replace bun-specific `toContainValue(x)` matcher with vitest `toContain(x)`
4. Update log string assertions from v3 Chinese strings to v4 English strings (see RESEARCH.md "v4 log strings to verify" section)
5. The v4 `JsonParser` constructor takes an optional `Logger` — create a simple mock logger that captures messages for log assertions
6. Failed parse cases (truncated strings, dangling keys) should expect `data: null` — NOT throw exceptions

Preserve the v3 test structure: two `describe` blocks ("ParseResult" with 6 cases, "JsonParser" with 12 cases across sub-describes).

Do NOT add new test cases beyond the 18 from v3 — per locked decision.
  </action>
  <verify>
    <automated>yarn workspace koishi-plugin-yesimbot vitest run src/services/agent/__tests__/json-parser.test.ts 2>&1 | tail -20</automated>
    <manual>All 18 tests pass</manual>
  </verify>
  <done>18 test cases pass, covering perfect JSON, code blocks, nested code blocks, OBSERVE prefix, truncated strings, and dangling keys</done>
</task>

</tasks>

<verification>
- `yarn workspace koishi-plugin-yesimbot vitest run` exits 0 with 18 tests passing
- No `console.log` in test file (use mock logger pattern)
- Test file imports from v4 `json-parser.ts`, not v3
</verification>

<success_criteria>
JSON parser has 18 passing vitest tests ported from v3, vitest is installed and runnable via `yarn workspace koishi-plugin-yesimbot test`.
</success_criteria>

<output>
After completion, create `.planning/phases/23-bug-fixes-reliability/23-01-SUMMARY.md`
</output>
