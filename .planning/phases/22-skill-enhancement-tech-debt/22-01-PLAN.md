---
phase: 22-skill-enhancement-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/src/services/skill/types.ts
  - core/src/services/skill/loader.ts
  - core/src/services/skill/service.ts
  - core/src/services/agent/loop.ts
autonomous: true
requirements:
  - SKILL-01
  - SKILL-02

must_haves:
  truths:
    - "A skill with injection_point: soul in its SKILL.md has its prompt content injected at the soul point during prompt assembly"
    - "A skill without injection_point in its SKILL.md defaults to extra point (backward compatible)"
    - "A skill with style_injection_point in its SKILL.md has its style override injected at that point instead of hardcoded soul"
    - "Multiple skills injecting to the same point are ordered by specificity (highest first)"
  artifacts:
    - path: "core/src/services/skill/types.ts"
      provides: "injectionPoint and styleInjectionPoint fields on SkillDefinition, point field on styleOverride"
      contains: "injectionPoint"
    - path: "core/src/services/skill/loader.ts"
      provides: "Frontmatter parsing and validation for injection_point and style_injection_point"
      contains: "validateInjectionPoint"
    - path: "core/src/services/skill/service.ts"
      provides: "mergeEffects uses skill.injectionPoint instead of hardcoded extra, sorts by specificity"
      contains: "skill.injectionPoint"
    - path: "core/src/services/agent/loop.ts"
      provides: "Style override reads point from effects instead of hardcoding soul"
      contains: "effects.styleOverride.point"
  key_links:
    - from: "core/src/services/skill/loader.ts"
      to: "core/src/services/skill/types.ts"
      via: "SkillDefinition.injectionPoint field populated from frontmatter"
      pattern: "injectionPoint.*validateInjectionPoint"
    - from: "core/src/services/skill/service.ts"
      to: "core/src/services/skill/types.ts"
      via: "mergeEffects reads injectionPoint from SkillDefinition"
      pattern: "skill\\.injectionPoint.*extra"
    - from: "core/src/services/agent/loop.ts"
      to: "core/src/services/skill/types.ts"
      via: "loop reads styleOverride.point instead of hardcoding soul"
      pattern: "effects\\.styleOverride\\.point"
---

<objective>
Make skill prompt effects configurable to target any of the 4 injection points (soul/instructions/memory/extra) instead of hardcoding "extra". Add style injection point routing. Sort prompt injections by specificity.

Purpose: Skills like private-chat need to inject at "soul" for personality adjustments, not "extra". This unlocks the full injection point system for skills.
Output: Updated types, loader, service, and loop files with configurable injection point routing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-skill-enhancement-tech-debt/22-RESEARCH.md
@core/src/services/skill/types.ts
@core/src/services/skill/loader.ts
@core/src/services/skill/service.ts
@core/src/services/agent/loop.ts
@core/src/services/prompt/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add injection point fields to types and update loader parsing</name>
  <files>
    core/src/services/skill/types.ts
    core/src/services/skill/loader.ts
  </files>
  <action>
**types.ts changes:**

1. Add two optional fields to `SkillDefinition` interface:
   - `injectionPoint?: InjectionPoint` — which injection point to target for prompt content (defaults to "extra" at merge time)
   - `styleInjectionPoint?: InjectionPoint` — which injection point for style override (defaults to "soul" at merge time)

2. Update `SkillEffect.styleOverride` type to include the injection point:
   - Change from `{ content: string; specificity: number } | null`
   - To `{ content: string; specificity: number; point: InjectionPoint } | null`

**loader.ts changes:**

1. Import `INJECTION_POINTS` and `InjectionPoint` from `../prompt/types`

2. Add a `validateInjectionPoint` helper function:
   ```typescript
   function validateInjectionPoint(
     val: unknown,
     logger: Logger,
     skillName: string,
   ): InjectionPoint | undefined {
     if (val == null) return undefined;
     if (typeof val === "string" && (INJECTION_POINTS as readonly string[]).includes(val)) {
       return val as InjectionPoint;
     }
     logger.warn("Invalid injection_point '%s' in skill %s, using default", val, skillName);
     return undefined;
   }
   ```

3. In `loadSkillsFromDir`, when building the `SkillDefinition` object, add:
   - `injectionPoint: validateInjectionPoint(meta.injection_point, logger, entry.name)`
   - `styleInjectionPoint: validateInjectionPoint(meta.style_injection_point, logger, entry.name)`

Note: Frontmatter uses snake_case (`injection_point`), TypeScript uses camelCase (`injectionPoint`). This matches existing pattern (e.g., `stickyTimeout` from `stickyTimeout` frontmatter).
  </action>
  <verify>
    <automated>yarn typecheck 2>&1 | tail -20</automated>
    <manual>Verify types.ts has both new fields on SkillDefinition and updated styleOverride type. Verify loader.ts has validateInjectionPoint and uses it.</manual>
  </verify>
  <done>SkillDefinition has injectionPoint and styleInjectionPoint optional fields. SkillEffect.styleOverride includes point field. Loader parses and validates both fields from SKILL.md frontmatter with graceful fallback on invalid values.</done>
</task>

<task type="auto">
  <name>Task 2: Update mergeEffects for configurable injection and fix loop style routing</name>
  <files>
    core/src/services/skill/service.ts
    core/src/services/agent/loop.ts
  </files>
  <action>
**service.ts mergeEffects() changes:**

1. Sort active skills by specificity descending before iterating (user-locked decision: higher specificity first):
   ```typescript
   const sorted = [...active].sort((a, b) => {
     const specA = a.conditions ? specificity(a.conditions) : 0;
     const specB = b.conditions ? specificity(b.conditions) : 0;
     return specB - specA;
   });
   ```

2. Replace hardcoded `point: "extra"` with `point: skill.injectionPoint ?? "extra"` in the prompt injection push.

3. Update bestStyle tracking to include the injection point:
   - Change `bestStyle` type from `{ content: string; specificity: number } | null` to `{ content: string; specificity: number; point: InjectionPoint } | null`
   - When setting bestStyle, include: `point: skill.styleInjectionPoint ?? "soul"`

4. Import `InjectionPoint` type if not already available through the types chain.

**loop.ts changes:**

1. Update the style override injection block (lines 81-88) to read the point from the effect instead of hardcoding "soul":
   ```typescript
   if (effects.styleOverride) {
     disposers.push(
       prompt.inject(this.ctx, effects.styleOverride.point, {
         name: `__skill_style_${percept.id}`,
         ...(effects.styleOverride.point === "soul"
           ? { after: "__role_soul" }
           : {}),
         renderFn: () => effects.styleOverride!.content,
       }),
     );
   }
   ```
   Key: Only set `after: "__role_soul"` when the target point is "soul" (where that anchor exists). For other points, omit `after` to append at end.
  </action>
  <verify>
    <automated>yarn typecheck 2>&1 | tail -20</automated>
    <manual>Verify service.ts mergeEffects sorts by specificity, uses skill.injectionPoint, and propagates styleInjectionPoint. Verify loop.ts reads effects.styleOverride.point.</manual>
  </verify>
  <done>mergeEffects() uses configurable injection points with correct defaults (extra for prompt, soul for style). Active skills sorted by specificity descending. Loop reads style injection point from effect instead of hardcoding "soul". Existing skills without injection_point fields continue to work identically (backward compatible).</done>
</task>

</tasks>

<verification>
1. `yarn typecheck` passes with no errors
2. Existing SKILL.md files (image-gen, mention-aware, private-chat) have no injection_point field and will default to "extra" for prompt and "soul" for style — backward compatible
3. The private-chat skill's style effect will still inject at "soul" by default (styleInjectionPoint not set → defaults to "soul")
4. No new dependencies added
</verification>

<success_criteria>
- SkillDefinition type has injectionPoint and styleInjectionPoint optional fields
- Loader validates injection_point and style_injection_point from SKILL.md frontmatter
- mergeEffects() reads injectionPoint from skill definition instead of hardcoding "extra"
- Style override carries its target injection point through to loop.ts
- Active skills sorted by specificity before prompt injection concatenation
- All existing skills work without modification (backward compatible defaults)
- yarn typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/22-skill-enhancement-tech-debt/22-01-SUMMARY.md`
</output>
