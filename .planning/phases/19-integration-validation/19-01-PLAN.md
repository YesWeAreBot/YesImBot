---
phase: 19-integration-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/src/services/agent/loop.ts
  - core/src/services/agent/tools.ts
  - core/src/services/agent/service.ts
  - core/src/services/trait/detectors/scene.ts
autonomous: true
requirements: [SKILL-05]

must_haves:
  truths:
    - "ThinkActLoop calls trait.analyze() and skill.resolve() between buildView and prompt rendering"
    - "Skill prompt injections and style overrides are applied as temporary injections with dispose cleanup"
    - "Tool filter from skill effects is passed to buildToolSchemaForPrompt and filters tool entries"
    - "AgentCore declares yesimbot.trait and yesimbot.skill as required dependencies"
    - "SceneTrait attaches triggerContent metadata to the scene signal for code activator consumption"
  artifacts:
    - path: "core/src/services/agent/loop.ts"
      provides: "Trait-Skill pipeline integration"
      contains: "trait.analyze"
    - path: "core/src/services/agent/tools.ts"
      provides: "Tool filter support"
      contains: "toolFilter"
    - path: "core/src/services/agent/service.ts"
      provides: "Updated inject dependencies"
      contains: "yesimbot.trait"
    - path: "core/src/services/trait/detectors/scene.ts"
      provides: "triggerContent in scene signal metadata"
      contains: "triggerContent"
  key_links:
    - from: "core/src/services/agent/loop.ts"
      to: "core/src/services/trait/service.ts"
      via: "ctx['yesimbot.trait'].analyze()"
      pattern: "trait\\.analyze"
    - from: "core/src/services/agent/loop.ts"
      to: "core/src/services/skill/service.ts"
      via: "ctx['yesimbot.skill'].resolve()"
      pattern: "skill\\.resolve"
    - from: "core/src/services/agent/loop.ts"
      to: "core/src/services/agent/tools.ts"
      via: "buildToolSchemaForPrompt with toolFilter"
      pattern: "buildToolSchemaForPrompt.*toolFilter"
---

<objective>
Wire the Trait-Skill pipeline into ThinkActLoop so that trait analysis and skill resolution happen between buildView() and prompt rendering, with skill effects applied as temporary injections.

Purpose: Enable the end-to-end Trait→Skill→Effect pipeline that Phase 18's SkillRegistry and Phase 17's TraitAnalyzer were built for.
Output: Modified loop.ts with pipeline integration, tools.ts with filter support, updated AgentCore inject, SceneTrait with triggerContent metadata.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-integration-validation/19-RESEARCH.md
@.planning/phases/18-skill-response/18-02-SUMMARY.md
@core/src/services/agent/loop.ts
@core/src/services/agent/tools.ts
@core/src/services/agent/service.ts
@core/src/services/trait/detectors/scene.ts
@core/src/services/skill/types.ts
@core/src/services/prompt/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add toolFilter to buildToolSchemaForPrompt and update AgentCore inject + SceneTrait metadata</name>
  <files>
    core/src/services/agent/tools.ts
    core/src/services/agent/service.ts
    core/src/services/trait/detectors/scene.ts
  </files>
  <action>
**tools.ts** — Add optional `toolFilter` parameter to `buildToolSchemaForPrompt`:
- New signature: `buildToolSchemaForPrompt(pluginService, toolCtx, toolFilter?)`
- `toolFilter` type: `{ include: string[]; exclude: string[] } | undefined`
- If `toolFilter` provided and `include.length > 0`, filter entries to only those whose `function.name` is in include
- If `toolFilter` provided and `exclude.length > 0`, filter out entries whose `function.name` is in exclude
- Apply include before exclude
- Import the `ToolFilter` type from `../skill/types` (it already has `include?: string[]` and `exclude?: string[]`)

**service.ts** — Update AgentCore static inject to add trait and skill dependencies:
- Change `static inject = ["yesimbot.horizon", "yesimbot.plugin", "yesimbot.prompt", "yesimbot.model"]`
- To: `static inject = ["yesimbot.horizon", "yesimbot.plugin", "yesimbot.prompt", "yesimbot.model", "yesimbot.trait", "yesimbot.skill"]`

**scene.ts** — Attach `triggerContent` to the scene signal metadata:
- In the `detect()` method, after creating the scene signal, find the last message from `view.history`
- If found, add `metadata: { triggerContent: lastMsg.content }` to the scene signal
- This enables code activators (like image-gen) to inspect message content via signal metadata
- Use: `const lastMsg = view.history?.filter(o => o.type === 'message').slice(-1)[0]`
  </action>
  <verify>Run `yarn typecheck` from repo root — must pass with no errors on modified files.</verify>
  <done>buildToolSchemaForPrompt accepts optional toolFilter, AgentCore declares trait/skill dependencies, SceneTrait scene signal carries triggerContent metadata.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Trait-Skill pipeline into ThinkActLoop.run()</name>
  <files>core/src/services/agent/loop.ts</files>
  <action>
In `ThinkActLoop.run()`, insert the trait-skill pipeline between `buildView()` (line ~50) and the tool schema injection (line ~55). The pipeline runs once per percept (not per loop round), per user decision.

**Add imports** at top of loop.ts:
- `import type { TraitAnalyzer } from "../trait/service";`
- `import type { SkillRegistry } from "../skill/service";`
- `import type { SkillEffect } from "../skill/types";`

**After `const toolCtxWithPercept = { ...toolCtx, percept };` (line 52), add pipeline:**

```typescript
// Trait-Skill pipeline: analyze context, resolve active skills
const trait = this.ctx["yesimbot.trait"] as TraitAnalyzer;
const skill = this.ctx["yesimbot.skill"] as SkillRegistry;
const signals = await trait.analyze(percept.scope, view);
const effects = skill.resolve(signals, percept.scope);

const disposers: Array<() => void> = [];

// Apply prompt injections from active skills
for (const inj of effects.promptInjections) {
  disposers.push(prompt.inject(this.ctx, inj.point, {
    name: `__skill_${inj.skillName}_${percept.id}`,
    renderFn: () => inj.content,
  }));
}

// Apply style override from highest-specificity skill
if (effects.styleOverride) {
  disposers.push(prompt.inject(this.ctx, "style", {
    name: `__skill_style_${percept.id}`,
    after: "__default_style",
    renderFn: () => effects.styleOverride!.content,
  }));
}
```

**Modify tool schema injection** to pass toolFilter and use percept.id-suffixed name:
- Change `buildToolSchemaForPrompt(pluginService, toolCtxWithPercept)` to `buildToolSchemaForPrompt(pluginService, toolCtxWithPercept, effects.toolFilter)`
- Change injection name from `"__loop_tool_schema"` to `` `__loop_tool_schema_${percept.id}` ``
- Remove the old `disposeInjection` variable; instead push the dispose into `disposers`

**Replace the try/finally block:**
- The existing `try { ... } finally { disposeInjection(); }` becomes `try { ... } finally { for (const d of disposers) d(); }`
- Move the entire existing try block body (systemPrompt rendering through loop end) inside the new try
- The finally block disposes ALL injections (skill prompts + style + tool schema)

**Important:** The `__default_basic_functions` injection has `before: "__loop_tool_schema"` constraint. Since we're changing the name to include percept.id, update the PromptService default injection name reference. Actually — looking at the code, `before: "__loop_tool_schema"` in prompt/service.ts constructor will no longer match. Fix: in prompt/service.ts, remove the `before: "__loop_tool_schema"` from `__default_basic_functions` injection, since the ordering is already correct by registration order (default registers first, loop schema registers later and naturally comes after).

Wait — actually the `before` constraint means "__default_basic_functions renders BEFORE __loop_tool_schema". With the name change, the constraint just becomes a no-op (target not found = ignored by resolveOrder). The default basic_functions will still render, just without explicit ordering relative to the tool schema. Since both are in the same injection point and the default registers first, registration order preserves correct ordering. So no change needed in prompt/service.ts — the stale `before` reference is harmless.

Do NOT modify prompt/service.ts.
  </action>
  <verify>Run `yarn typecheck` from repo root — must pass. Run `yarn build` — must succeed. Verify the pipeline flow: buildView → trait.analyze → skill.resolve → inject effects → buildToolSchemaForPrompt(with filter) → renderToString → loop → finally dispose all.</verify>
  <done>ThinkActLoop.run() invokes trait analysis and skill resolution once per percept, applies prompt/style/tool effects as temporary injections, and cleans up all injections in finally block. Existing loop behavior (willingness gate, tool calling, memory injection) unchanged.</done>
</task>

</tasks>

<verification>
1. `yarn typecheck` passes with no errors
2. `yarn build` succeeds
3. In loop.ts: trait.analyze() called after buildView(), skill.resolve() called after analyze(), effects applied before renderToString()
4. All skill injections use percept.id suffix for concurrent safety
5. All injections disposed in finally block
6. AgentCore.inject includes "yesimbot.trait" and "yesimbot.skill"
7. buildToolSchemaForPrompt accepts and applies optional toolFilter
8. SceneTrait scene signal includes triggerContent metadata from last message
</verification>

<success_criteria>
- ThinkActLoop has a working Trait→Skill→Effect pipeline between buildView and prompt rendering
- Tool filtering works with include/exclude semantics
- All temporary injections are cleaned up on loop exit
- typecheck and build pass
</success_criteria>

<output>
After completion, create `.planning/phases/19-integration-validation/19-01-SUMMARY.md`
</output>
