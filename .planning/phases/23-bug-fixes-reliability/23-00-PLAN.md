---
phase: 23-bug-fixes-reliability
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - core/src/services/horizon/__tests__/format-horizon-text.test.ts
  - core/src/services/agent/__tests__/token-bucket.test.ts
  - core/src/services/agent/__tests__/willingness.test.ts
autonomous: true
requirements:
  - BUGFIX-01
  - WILL-01
  - WILL-02

must_haves:
  truths:
    - "format-horizon-text test asserts {{date.now}} and {{bot.name}} render non-empty after formatHorizonText call"
    - "token-bucket test asserts consume returns true until capacity exhausted, then false, and refills over time"
    - "willingness test asserts directBoost is applied when trigger type is 'direct'"
  artifacts:
    - path: "core/src/services/horizon/__tests__/format-horizon-text.test.ts"
      provides: "Smoke tests for snippet variable rendering in horizon-view"
      contains: "date.now"
    - path: "core/src/services/agent/__tests__/token-bucket.test.ts"
      provides: "Unit tests for TokenBucket consume/refill behavior"
      contains: "TokenBucket"
    - path: "core/src/services/agent/__tests__/willingness.test.ts"
      provides: "Unit tests for directBoost on DM trigger type"
      contains: "directBoost"
  key_links:
    - from: "core/src/services/horizon/__tests__/format-horizon-text.test.ts"
      to: "core/src/services/horizon/service.ts"
      via: "imports formatHorizonText or HorizonService"
      pattern: "import.*horizon"
    - from: "core/src/services/agent/__tests__/token-bucket.test.ts"
      to: "core/src/services/agent/willingness.ts"
      via: "imports TokenBucket"
      pattern: "import.*TokenBucket"
    - from: "core/src/services/agent/__tests__/willingness.test.ts"
      to: "core/src/services/agent/willingness.ts"
      via: "imports WillingnessEngine or processMessage"
      pattern: "import.*willingness"
---

<objective>
Create test scaffolds for BUGFIX-01, WILL-01, and WILL-02 before implementation plans run. These are RED tests — they import the target modules and assert expected behavior that does not yet exist. They will fail until the corresponding implementation plans (23-02, 23-03) make them pass.

Purpose: Satisfy Nyquist sampling — every implementation task must have an automated behavioral verify beyond typecheck.
Output: Three test files that fail (RED) but become green after 23-02 and 23-03 execute.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-bug-fixes-reliability/23-RESEARCH.md
@core/src/services/horizon/service.ts
@core/src/services/agent/willingness.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create format-horizon-text smoke test</name>
  <files>core/src/services/horizon/__tests__/format-horizon-text.test.ts</files>
  <action>
Create `core/src/services/horizon/__tests__/format-horizon-text.test.ts` with smoke tests for BUGFIX-01.

The test file should:
1. Import `{ describe, it, expect } from "vitest"`
2. Import `HorizonService` from `../service` (or the relevant export)
3. Create a minimal mock `HorizonView` with `self: { name: "TestBot", id: "bot-1" }` and a stub `environment`
4. Test: "{{date.now}} renders non-empty" — call `formatHorizonText(view)` and assert the output does NOT contain the literal string `{{date.now}}` (meaning it was resolved)
5. Test: "{{bot.name}} renders non-empty" — assert the output does NOT contain the literal string `{{bot.name}}` and DOES contain "TestBot"
6. Test: "missing sender variables preserve tag text" — call `formatHorizonText(view)` without percept and assert `{{sender.name}}` is preserved as literal text (not empty string)

These tests will FAIL (RED) until 23-02 implements the scope construction. That is expected.

If `HorizonService` cannot be instantiated without a full Koishi context, use a minimal approach:
- Extract the scope-building logic into a testable pure function, OR
- Mock the minimal context needed (just `ctx.logger`)
- The executor should inspect `service.ts` to determine the simplest testable surface

Do NOT use `console.log`. Use vitest's built-in assertion messages.
  </action>
  <verify>
    <automated>cd /home/workspace/Athena && yarn workspace koishi-plugin-yesimbot vitest run src/services/horizon/__tests__/format-horizon-text.test.ts 2>&1 | tail -10</automated>
    <manual>Test file exists and vitest discovers it (tests may fail RED — that is expected at Wave 0)</manual>
  </verify>
  <done>format-horizon-text.test.ts exists with at least 3 smoke tests asserting snippet variable rendering behavior</done>
</task>

<task type="auto">
  <name>Task 2: Create token-bucket and willingness test scaffolds</name>
  <files>core/src/services/agent/__tests__/token-bucket.test.ts, core/src/services/agent/__tests__/willingness.test.ts</files>
  <action>
Create two test files:

**1. `core/src/services/agent/__tests__/token-bucket.test.ts`** — unit tests for WILL-02:

```typescript
import { describe, it, expect } from "vitest";
import { TokenBucket } from "../willingness";
```

Test cases:
- "consume returns true until capacity exhausted" — create `TokenBucket(3, 1)`, call `consume("user-1")` 3 times (all true), 4th call returns false
- "different keys have independent buckets" — consume key "a" to exhaustion, key "b" still returns true
- "refills over time" — exhaust bucket, advance time (use `vi.useFakeTimers()` + `vi.advanceTimersByTime(2000)`), consume again returns true

**2. `core/src/services/agent/__tests__/willingness.test.ts`** — unit test for WILL-01 directBoost:

```typescript
import { describe, it, expect } from "vitest";
```

Test case:
- "directBoost applied for direct trigger type" — import `WillingnessEngine` (or the relevant processMessage function), create a minimal instance, call with `triggerType: "direct"`, assert returned probability is >= 0.9 (reflecting the 0.95 directBoost)

If `WillingnessEngine` requires a full Koishi context to instantiate, the executor should:
- Mock the minimal context (`ctx.logger`, `ctx.config`)
- Or test the `applyMentionBoost` helper directly with the directBoost value

These tests will FAIL (RED) until 23-03 implements TokenBucket and directBoost. That is expected.

Do NOT use `console.log`.
  </action>
  <verify>
    <automated>cd /home/workspace/Athena && yarn workspace koishi-plugin-yesimbot vitest run src/services/agent/__tests__/token-bucket.test.ts src/services/agent/__tests__/willingness.test.ts 2>&1 | tail -10</automated>
    <manual>Both test files exist and vitest discovers them (tests may fail RED — expected at Wave 0)</manual>
  </verify>
  <done>token-bucket.test.ts has 3 tests (exhaust, independent keys, refill); willingness.test.ts has 1 test (directBoost for direct trigger)</done>
</task>

</tasks>

<verification>
- All three test files exist and are discovered by vitest
- Tests are expected to FAIL (RED) at this stage — they assert behavior not yet implemented
- No `console.log` in any test file
- Test files import from the correct v4 source paths
</verification>

<success_criteria>
Three test scaffold files exist covering BUGFIX-01 (snippet rendering), WILL-01 (directBoost), and WILL-02 (token bucket). Vitest discovers all files. Tests fail RED — implementation plans 23-02 and 23-03 will make them green.
</success_criteria>

<output>
After completion, create `.planning/phases/23-bug-fixes-reliability/23-00-SUMMARY.md`
</output>
