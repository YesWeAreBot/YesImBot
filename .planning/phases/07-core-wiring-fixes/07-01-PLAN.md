---
phase: 07-core-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugins/core/src/services/prompt/service.ts
autonomous: true
requirements:
  - AGENT-01
  - PROMPT-01

must_haves:
  truths:
    - "LLM receives a non-empty system prompt even when user provides no custom template"
    - "PromptService logs a warning when any template renders to empty string"
    - "User-provided config.templates.system still overrides the default template"
  artifacts:
    - path: "plugins/core/src/services/prompt/service.ts"
      provides: "DEFAULT_SYSTEM_TEMPLATE constant and empty-render warnings"
      contains: "DEFAULT_SYSTEM_TEMPLATE"
  key_links:
    - from: "plugins/core/src/services/prompt/service.ts"
      to: "this.templates Map"
      via: "registerTemplate('system', DEFAULT_SYSTEM_TEMPLATE) in constructor"
      pattern: "registerTemplate.*system.*DEFAULT_SYSTEM_TEMPLATE"
    - from: "plugins/core/src/services/prompt/service.ts"
      to: "logger warn"
      via: "warn call after empty render result"
      pattern: "logger\\.warn.*empty"
---

<objective>
Bundle a default "system" Mustache template into PromptService and add empty-render warnings.

Purpose: Close two gaps from v1 milestone audit — the LLM should never receive an empty system prompt when no user template is configured, and PromptService should warn when render() produces empty output for debugging.
Output: Updated service.ts with DEFAULT_SYSTEM_TEMPLATE and warn logging.
</objective>

<execution_context>
@C:/Users/MiaowFISH/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MiaowFISH/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-core-wiring-fixes/07-CONTEXT.md
@.planning/phases/07-core-wiring-fixes/07-RESEARCH.md
@plugins/core/src/services/prompt/service.ts
@plugins/core/src/services/agent/loop.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DEFAULT_SYSTEM_TEMPLATE and register in constructor</name>
  <files>plugins/core/src/services/prompt/service.ts</files>
  <action>
Add a `DEFAULT_SYSTEM_TEMPLATE` string constant above the class definition. Content structure per locked decisions:

- Three XML-tagged modules: `<identity>`, `<style>`, `<how_you_work>`
- Language: structural XML tags in English, instruction content in Chinese
- Mustache variables: `{{view.self.name}}` for bot name, `{{#view.environment}}...{{/view.environment}}` conditional block for environment context with `{{view.environment.name}}`
- Trimmed from dev version identity.mustache — keep core directives, remove overly specific behaviors (typos, self-deprecation, inner_voice section)
- Do NOT include memory/tools/output modules (handled by Horizon view and ai-sdk)

In the PromptService constructor, after `this.config = config`, call:

```
this.registerTemplate("system", DEFAULT_SYSTEM_TEMPLATE);
```

This ensures the default is always available as fallback. The existing resolution order (`config.templates?.[name] ?? this.templates.get(name)`) means user config still wins — no changes needed to render().
</action>
<verify>
Run `npx turbo run build --filter=@yesimbot/core` — must compile without errors. Verify DEFAULT_SYSTEM_TEMPLATE contains `{{view.self.name}}` and `{{#view.environment}}` by reading the file.
</verify>
<done>
DEFAULT_SYSTEM_TEMPLATE constant exists with identity/style/how_you_work modules. Constructor registers it as "system". Build passes.
</done>
</task>

<task type="auto">
  <name>Task 2: Add logger field and empty-render warnings</name>
  <files>plugins/core/src/services/prompt/service.ts</files>
  <action>
Add a private logger field to PromptService (per CLAUDE.md — create logger once, then use it):
```
private logger = this.ctx.logger("yesimbot.prompt");
```

In `render()`, add warn logging at two points:

1. When no template content is found (the existing early return at line 46):
   - Before `return ""`, add: `this.logger.warn(\`No template found for key "${templateName}"\`);`

2. After `this.renderer.render(...)` returns, check if result is empty:
   - `const result = this.renderer.render(templateContent, scope, partials);`
   - `if (!result) this.logger.warn(\`Template "${templateName}" rendered to empty string\`);`
   - `return result;`

Per locked decisions: warn only, do NOT fallback to default template. Caller (ThinkActLoop) decides how to handle empty prompt.
</action>
<verify>
Run `npx turbo run build --filter=@yesimbot/core` — must compile without errors. Read service.ts and confirm both warn paths exist.
</verify>
<done>
PromptService has a private logger field. render() warns on both "no template found" and "rendered to empty string" cases. Build passes.
</done>
</task>

</tasks>

<verification>
1. `npx turbo run build --filter=@yesimbot/core` passes
2. service.ts contains DEFAULT_SYSTEM_TEMPLATE with `{{view.self.name}}` and `{{#view.environment}}`
3. Constructor calls `this.registerTemplate("system", DEFAULT_SYSTEM_TEMPLATE)`
4. render() has warn log for missing template AND for empty render result
5. Private logger field exists (not inline `ctx.logger()` calls)
</verification>

<success_criteria>

- Build succeeds with no type errors
- DEFAULT_SYSTEM_TEMPLATE is registered as "system" in constructor
- PromptService.render() warns on empty output (both paths)
- No changes to render() resolution logic (user config still overrides default)
  </success_criteria>

<output>
After completion, create `.planning/phases/07-core-wiring-fixes/07-01-SUMMARY.md`
</output>
