---
phase: 13-non-stream-path-fallback-wiring
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - plugins/core/src/services/agent/loop.ts
  - plugins/core/src/services/agent/tools.ts
autonomous: true
requirements:
  - AGENT-01
  - AGENT-03

must_haves:
  truths:
    - "Non-stream path calls modelService.call() instead of raw generateText()"
    - "fallbackModel from config is passed to modelService.call()"
    - "No defaultParams extraction or spread in loop.ts — ModelService handles it"
    - "finishTool cannot be overwritten by a plugin tool with the same name"
    - "finishTool is added after plugin tools, guarding the 'finish' key"
  artifacts:
    - path: "plugins/core/src/services/agent/loop.ts"
      provides: "Non-stream path through ModelService.call()"
      contains: "modelService.call"
    - path: "plugins/core/src/services/agent/tools.ts"
      provides: "finishTool added last with collision guard"
      contains: "finish"
  key_links:
    - from: "plugins/core/src/services/agent/loop.ts"
      to: "plugins/core/src/services/model/service.ts"
      via: "modelService.call() for non-stream"
      pattern: "modelService\\.call"
---

<objective>
Rewire the non-stream path in ThinkActLoop to use modelService.call() and pass config.fallbackModel. Clean up finishTool double-inclusion risk in buildAiSdkTools.

Purpose: Complete the ModelService gateway pattern — both stream and non-stream paths gain PQueue concurrency control and fallback chain.
Output: Updated loop.ts and tools.ts.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-non-stream-path-fallback-wiring/13-CONTEXT.md
@.planning/phases/13-non-stream-path-fallback-wiring/13-01-SUMMARY.md
@plugins/core/src/services/agent/loop.ts
@plugins/core/src/services/agent/tools.ts
@plugins/core/src/services/model/service.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire non-stream path to modelService.call() and pass fallbackModel</name>
  <files>plugins/core/src/services/agent/loop.ts</files>
  <action>
1. **Remove** the `generateText` import from `ai` (no longer needed in loop.ts). Remove the `parseModelId` import from shared-model (no longer needed).

2. **Remove line 51** (`const { model, defaultParams } = modelService.getModel(config.model ?? "") ?? {};`) — ModelService.call() handles model resolution and default params internally.

3. **Remove defaultParams spread from callParams** (line 74): The callParams object should NOT include `...defaultParams`. Build callParams from only the loop's own fields:
   ```ts
   const callParams = {
     system: systemPrompt,
     messages,
     tools: allTools as ToolSet,
     toolChoice: "required",
     stopWhen,
     onStepFinish,
   } as CallParams;
   ```

4. **Replace the non-stream branch** (line 92): Instead of `generateText({ model, ...callParams })`, call:
   ```ts
   const result = await Promise.race([
     modelService.call(config.model ?? "", callParams, config.fallbackModel),
     timeoutPromise,
   ]);
   resultText = result?.text;
   ```

5. **Update stream branch** to also pass fallbackModel:
   ```ts
   const streamResult = await Promise.race([
     modelService.streamCall(config.model ?? "", callParams, config.fallbackModel),
     timeoutPromise,
   ]);
   ```

6. Remove any now-unused imports (generateText, parseModelId, LanguageModel if present).
  </action>
  <verify>Run `yarn typecheck` — no errors in plugins/core.</verify>
  <done>
    - Non-stream path uses modelService.call() with fallbackModel
    - Stream path passes fallbackModel to modelService.streamCall()
    - No defaultParams extraction or spread in loop.ts
    - No direct generateText import in loop.ts
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix finishTool collision guard in buildAiSdkTools</name>
  <files>plugins/core/src/services/agent/tools.ts</files>
  <action>
In `buildAiSdkTools`:

1. **Initialize tools as empty object** instead of `{ finish: finishTool }`:
   ```ts
   const tools: ToolSet = {};
   ```

2. **After the plugin tool loop**, add finishTool last so it cannot be overwritten:
   ```ts
   tools["finish"] = finishTool;
   ```

This ensures finishTool is always present and no plugin can accidentally shadow it. The single assembly point is now: plugin tools first, then finishTool appended.
  </action>
  <verify>Run `yarn typecheck` — no errors. Verify finishTool assigned after loop in tools.ts.</verify>
  <done>
    - finishTool added after plugin tools, cannot be overwritten
    - Tool assembly has single clear flow: plugins -> finishTool
  </done>
</task>

</tasks>

<verification>
1. `yarn typecheck` passes for plugins/core
2. loop.ts has no `generateText` import and no `getModel()` call
3. loop.ts calls `modelService.call(model, params, fallbackModel)` for non-stream
4. loop.ts calls `modelService.streamCall(model, params, fallbackModel)` for stream
5. tools.ts assigns finishTool after the plugin tool iteration loop
</verification>

<success_criteria>
- Both stream and non-stream paths go through ModelService with fallbackModel
- No raw generateText/streamText calls in loop.ts
- No defaultParams double-merge risk
- finishTool always wins over plugin tools with same name
- typecheck passes
</success_criteria>

<output>
After completion, create `.planning/phases/13-non-stream-path-fallback-wiring/13-02-SUMMARY.md`
</output>
