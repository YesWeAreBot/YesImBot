---
phase: 21-fixed-role-file-loading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - core/resources/roles/SOUL.md
  - core/resources/roles/AGENTS.md
  - core/resources/roles/TOOLS.md
autonomous: true
requirements: [ROLE-04]

must_haves:
  truths:
    - "Bundled SOUL.md defines bot personality with Markdown heading structure and Mustache variables"
    - "Bundled AGENTS.md defines behavioral instructions with Markdown heading structure"
    - "Bundled TOOLS.md defines tool usage guidance with Markdown heading structure"
    - "All three files use {{bot.name}} and {{date.now}} template variables where appropriate"
  artifacts:
    - path: "core/resources/roles/SOUL.md"
      provides: "Default bot personality and identity content"
      contains: "{{bot.name}}"
    - path: "core/resources/roles/AGENTS.md"
      provides: "Default behavioral instructions and control flow"
      contains: "{{bot.name}}"
    - path: "core/resources/roles/TOOLS.md"
      provides: "Default tool usage guidance"
      contains: "actions"
  key_links: []
---

<objective>
Write bundled default SOUL.md, AGENTS.md, and TOOLS.md role files that define the bot's personality, behavioral instructions, and tool usage guidance.

Purpose: These files are the content that RoleService (Plan 02) will load and inject into the soul and instructions injection points, replacing the deleted default-identity.md, default-style.md, default-control-flow.md, and default-basic-functions.md files.

Output: Three Markdown files in core/resources/roles/ with Mustache template variables and ## heading structure.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/21-fixed-role-file-loading/21-CONTEXT.md
@.planning/phases/21-fixed-role-file-loading/21-RESEARCH.md

# Reference materials for content design
@references/openclaw/docs/reference/templates/SOUL.md
@references/openclaw/docs/reference/templates/AGENTS.md
@references/openclaw/docs/reference/templates/TOOLS.md
@references/letta/letta/prompts/system_prompts/memgpt_v2_chat.py

# v3 reference for proven prompt lineage
@references/YesImBot-v3/
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write bundled default SOUL.md and AGENTS.md</name>
  <files>core/resources/roles/SOUL.md, core/resources/roles/AGENTS.md</files>
  <action>
Create `core/resources/roles/` directory and write two files.

**SOUL.md** — Injected at the `soul` point. Defines WHO the bot is. Content guidelines:
- Open with a brief identity statement using `{{bot.name}}` (e.g. "You are {{bot.name}}, ...")
- Use `## sections` for: Identity, Personality, Communication Style, Boundaries
- Draw from letta's memgpt_v2_chat immersion philosophy: "completely and entirely immerse yourself in your persona, think like them, act like them, talk like them"
- Draw from OpenClaw SOUL.md's anti-sycophancy: "Skip the 'Great question!' and 'I'd be happy to help!' — just help"
- Include `{{date.now}}` in a contextual awareness section
- Language: English. Tone: natural, conversational, not corporate
- Keep concise — this is a starting point users will customize
- Use Markdown headings (## sections) for future RAG chunking compatibility
- Do NOT use emojis

**AGENTS.md** — Injected at the `instructions` point. Defines HOW the bot behaves. Content guidelines:
- Use `## sections` for: Control Flow, Response Format, Inner Monologue, Memory Awareness, Group Chat Behavior
- Draw from letta's control_flow concept: brain runs in short bursts, event-driven, heartbeat continuation
- Draw from letta's basic_functions: inner monologue for planning and reflection before action
- Draw from OpenClaw AGENTS.md's group chat wisdom: "Participate, don't dominate", know when to speak vs stay silent
- Include the JSON response format with thoughts/actions structure (this is what the ThinkActLoop expects)
- Reference `{{bot.name}}` where appropriate
- Keep concise and actionable — instructions, not essays
- Do NOT use emojis

Per user decision: "Borrow OpenClaw's file-separation philosophy but design Athena's own voice and structure." These are Athena's own defaults, not copies.
  </action>
  <verify>
    <automated>test -f core/resources/roles/SOUL.md && test -f core/resources/roles/AGENTS.md && grep -q '{{bot.name}}' core/resources/roles/SOUL.md && grep -q '{{bot.name}}' core/resources/roles/AGENTS.md && echo "PASS"</automated>
    <manual>Review content for natural tone, proper Mustache variables, and ## heading structure</manual>
  </verify>
  <done>SOUL.md and AGENTS.md exist with Mustache variables, ## heading structure, and content inspired by letta/OpenClaw but in Athena's own voice</done>
</task>

<task type="auto">
  <name>Task 2: Write bundled default TOOLS.md</name>
  <files>core/resources/roles/TOOLS.md</files>
  <action>
Write `core/resources/roles/TOOLS.md` — Also injected at the `instructions` point (after AGENTS.md). Defines tool usage guidance.

Content guidelines:
- Use `## sections` for: Tool Calling, Response Format, Available Actions
- Explain the JSON action format: `{"name": "tool_name", "params": {...}}`
- Explain the Tool vs Action distinction (Tools return results for further processing, Actions have side effects like sending messages)
- Mention `send_message` as the primary action for responding to users
- Mention `request_heartbeat: true` for chaining tool calls
- Reference the thoughts/actions JSON structure that ThinkActLoop parses
- Keep it focused on HOW to use tools, not WHAT tools exist (dynamic tool schema is injected per-turn by loop.ts)
- Do NOT use emojis

This file is optional (ROLE-03) — users can delete it. But the bundled default provides useful guidance for the LLM.
  </action>
  <verify>
    <automated>test -f core/resources/roles/TOOLS.md && grep -q 'send_message' core/resources/roles/TOOLS.md && echo "PASS"</automated>
  </verify>
  <done>TOOLS.md exists with tool usage guidance, JSON format explanation, and Tool/Action distinction</done>
</task>

</tasks>

<verification>
1. All three files exist in core/resources/roles/
2. All files use ## Markdown headings
3. SOUL.md and AGENTS.md contain {{bot.name}} template variable
4. SOUL.md contains {{date.now}} template variable
5. Content is in English, natural tone, no emojis
6. Files are self-contained Markdown (no frontmatter needed — these are pure content files)
</verification>

<success_criteria>
Three bundled default role files exist with Mustache template variables, Markdown heading structure, and content that draws from letta/OpenClaw references while establishing Athena's own voice.
</success_criteria>

<output>
After completion, create `.planning/phases/21-fixed-role-file-loading/21-01-SUMMARY.md`
</output>
