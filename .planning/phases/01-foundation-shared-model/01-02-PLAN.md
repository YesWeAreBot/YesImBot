---
phase: 01-foundation-shared-model
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - plugins/core/package.json
  - plugins/core/tsconfig.json
  - plugins/core/src/index.ts
autonomous: true
requirements:
  - PLATFORM-01

must_haves:
  truths:
    - "Core plugin can be loaded by Koishi 4.x (exports name, Config, apply)"
    - "Core plugin depends on @yesimbot/shared-model via workspace protocol"
    - "Core plugin has ready/dispose lifecycle hooks"
    - "TypeScript compilation works across shared-model and core with proper module resolution"
    - "turbo run build succeeds for all packages"
  artifacts:
    - path: "plugins/core/src/index.ts"
      provides: "Koishi plugin entry with apply function, Config schema, lifecycle hooks"
      contains: "export function apply"
    - path: "plugins/core/package.json"
      provides: "Package manifest as @yesimbot/koishi-plugin-core"
      contains: "koishi-plugin-core"
  key_links:
    - from: "plugins/core/package.json"
      to: "@yesimbot/shared-model"
      via: "workspace:* dependency"
      pattern: "workspace:\\*"
    - from: "plugins/core/tsconfig.json"
      to: "packages/shared-model"
      via: "TypeScript project reference"
      pattern: "shared-model"
---

<objective>
Create the Koishi 4.x core plugin skeleton that serves as the main entry point for YesImBot.

Purpose: Establish the plugin structure that Koishi can load, with proper lifecycle management and dependency on shared-model. This is the shell that Phase 2+ will fill with ModelService, AgentCore, and other services.
Output: Loadable @yesimbot/koishi-plugin-core package with Koishi plugin structure.
</objective>

<execution_context>
@C:/Users/MiaowFISH/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/MiaowFISH/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-shared-model/01-CONTEXT.md
@.planning/phases/01-foundation-shared-model/01-RESEARCH.md
@.planning/phases/01-foundation-shared-model/01-01-SUMMARY.md
@package.json
@tsconfig.base.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create @yesimbot/koishi-plugin-core package</name>
  <files>
    plugins/core/package.json
    plugins/core/tsconfig.json
    plugins/core/src/index.ts
  </files>
  <action>
Create `plugins/core/` with Koishi 4.x plugin structure:

**package.json**: Name `@yesimbot/koishi-plugin-core`. Dependencies: `@yesimbot/shared-model: "workspace:*"`. PeerDependencies: `koishi: "^4.0.0"`. Use pkgroll for build (same pattern as shared-model). Set main/module/types fields.

**tsconfig.json**: Extend `../../tsconfig.base.json`. Set `outDir: "./dist"`, `rootDir: "./src"`. Add project reference to `../../packages/shared-model`. Include `["src/**/*"]`.

**src/index.ts**: Standard Koishi plugin entry per locked decisions:
- Export `name = 'yesimbot-core'`
- Export `inject = []` (no service dependencies initially)
- Export `Config` interface (empty for now) and `Config` Schema (Schema.object({}))
- Export `apply(ctx: Context, config: Config)` function with:
  - `ctx.on('ready', ...)` logging initialization
  - `ctx.on('dispose', ...)` logging cleanup
- Import `Context` and `Schema` from 'koishi'

Per locked decision: core plugin contains all built-in services (stubs for now — ModelService, AgentCore, Horizon will be added in later phases). Provider registration via custom registry will be added in Phase 2.

Do NOT add adapter layer, ModelService, or provider registry yet — those belong to later phases. This is skeleton only.
  </action>
  <verify>
Run `turbo run build` to verify all packages build successfully (shared-model first, then core).
Run `cd plugins/core && npx tsc --noEmit` to verify TypeScript compilation with project references.
Verify core's package.json has `@yesimbot/shared-model: "workspace:*"` in dependencies.
  </verify>
  <done>
Core plugin builds successfully, exports valid Koishi plugin structure (name, Config, apply), depends on shared-model via workspace protocol, full monorepo builds with turbo run build.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify full monorepo build and install</name>
  <files></files>
  <action>
Run `yarn install` to link all workspace packages, then `turbo run build` to verify the complete build pipeline:
1. shared-model builds first (no dependencies)
2. core builds second (depends on shared-model via ^build)
3. Both produce dist/ output
4. No TypeScript errors across packages

If yarn install fails due to missing providers/ packages, that's expected (empty workspace dir). Verify the .gitkeep approach works or adjust if needed.

Check that `turbo run build` output shows correct task ordering and caching behavior.
  </action>
  <verify>
`yarn install` completes without errors.
`turbo run build` shows 2 successful tasks (shared-model, core) in correct order.
`ls packages/shared-model/dist/` shows compiled output.
`ls plugins/core/dist/` shows compiled output.
  </verify>
  <done>
Full monorepo builds successfully: yarn install links workspaces, turbo run build compiles shared-model then core, both produce dist/ artifacts.
  </done>
</task>

</tasks>

<verification>
1. `turbo run build` succeeds with 2 packages in correct dependency order
2. Core plugin exports: name, Config (Schema), apply function
3. Core plugin has workspace:* dependency on shared-model
4. TypeScript project references resolve correctly
5. Both packages produce dist/ output with .d.ts type declarations
</verification>

<success_criteria>
- Koishi core plugin skeleton exists with proper apply/Config/name exports
- Full monorepo builds end-to-end with turbo run build
- TypeScript compilation works across package boundaries
- workspace:* protocol correctly links shared-model to core
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-shared-model/01-02-SUMMARY.md`
</output>
